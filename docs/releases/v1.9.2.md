# onec-mcp-universal v1.9.2

## Исправления

- Исправлен CI-only дефект в `tests/test_docker_manager.py::test_start_lsp_skips_when_image_missing`.
- Тест больше не пытается создавать путь вида `/home/as/...` на GitHub runner и явно подменяет `os.makedirs` и `_resolve_lsp_mount_path`.
- Runtime-код `v1.9.1` не менялся; релиз содержит только стабилизацию test suite для clean CI.

## Проверка

- локально: `973 passed`
- локально: `line+branch coverage = 100.00%`
- GitHub `CI` должен проходить на чистом runner без зависимостей от локальной файловой системы разработчика

## Образы GHCR

- `ghcr.io/alekseiseleznev/onec-mcp-universal:1.9.2`
- `ghcr.io/alekseiseleznev/onec-mcp-universal-bsl-graph-lite:1.9.2`
- `ghcr.io/alekseiseleznev/onec-mcp-universal-docker-control:1.9.2`
