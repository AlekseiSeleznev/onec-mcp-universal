# onec-mcp-universal v1.8.0

## Что вошло

- полный Codex-first аудит репозитория и install flow
- новый `CODEX.md` как исчерпывающий runbook для установки и эксплуатации под Codex
- `AGENTS.md` переведён в нейтральный AI-agnostic режим
- из tracked-репозитория убраны legacy-упоминания других AI-клиентов
- `README.md` и dashboard docs (`ru` / `en`) синхронизированы с текущей реализацией
- `setup.sh` и verify flow оставлены `Codex-first`: при наличии `codex` сервер регистрируется автоматически
- CI coverage gate поднят до `100%`
- локальный `gateway/scripts/test.sh` теперь по умолчанию запускает тесты с coverage gate `100%`

## Технические изменения

- тестовый контур расширен до полного покрытия строк и ветвей для пакета `gateway`
- закрыты runtime-хвосты в `gateway/server.py`: lifespan loops, reconnect background path, `.env` fallback branches, reindex branches
- asset-checks обновлены под `CODEX.md`, нейтральный `AGENTS.md`, отсутствие legacy AI references и CI gate `100%`
- версия пакета и UI/docs обновлена до `v1.8.0`

## Проверка

- локально: `924 passed`
- coverage: `TOTAL 100%`
- repo-wide grep по legacy AI references: пустой результат

## Совместимость

- MCP endpoint не меняется: `http://localhost:8080/mcp`
- `./setup.sh` остаётся основным entrypoint
- `./setup.sh --with-bsl-graph` остаётся поддержанным one-command путём для bundled graph profile
