# onec-mcp-universal v1.8.5

## Исправления

- `Docker Build & Publish` переведён с JavaScript Docker actions на shell-based Docker CLI.
- Убраны deprecation-аннотации GitHub Actions по Node 20 для docker workflow.
- Исправлено имя gateway-образа в GHCR на нижний регистр для shell-based publish.

## Проверка

- локальные asset-тесты для workflow-конфигурации обновлены;
- GitHub `CI` и `Docker Build & Publish` должны проходить без предупреждения о Node 20 deprecation и без ошибки `repository name must be lowercase`.
