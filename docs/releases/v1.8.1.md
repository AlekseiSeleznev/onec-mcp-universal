# onec-mcp-universal v1.8.1

## Что вошло

- исправлен CI-only дефект в тестах `docker_manager`, из-за которого GitHub Actions пытался создавать `/home/as/...` на раннере
- подтверждено повторным полным прогоном, что coverage gate `100%` остаётся зелёным и локально, и в CI
- сохранены все изменения релизной линии `v1.8.0`: Codex-first install flow, `CODEX.md`, нейтральный `AGENTS.md`, русский `README.md`, bundled `bsl-graph`

## Технические изменения

- `gateway/tests/test_docker_manager.py`: тест `test_start_lsp_returns_existing_container_without_changes` теперь мокает `os.makedirs` так же, как соседние сценарии `start_lsp`
- версия пакета и UI/docs обновлена до `v1.8.1`

## Проверка

- локально: `924 passed`
- coverage: `TOTAL 100%`
- clean reinstall из GitHub повторён после фикса

## Совместимость

- MCP endpoint не меняется: `http://localhost:8080/mcp`
- `./setup.sh` остаётся основным entrypoint
- `./setup.sh --with-bsl-graph` остаётся поддержанным one-command путём для bundled graph profile
