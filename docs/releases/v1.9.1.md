# onec-mcp-universal v1.9.1

## Исправления

- `gateway/requirements-dev.txt` теперь явно включает `docker>=7.1.0,<8.0.0`.
- Исправлен CI-only дефект `ModuleNotFoundError: No module named 'docker'` в `tests/test_docker_manager.py` после удаления Docker SDK из runtime-зависимостей gateway.
- Runtime-контракт `v1.9.0` не менялся:
  - gateway по-прежнему не получает `docker.sock`
  - `docker-control` остаётся единственной точкой Docker lifecycle
  - `NAPARNIK_API_KEY` не возвращается в container env
  - safe-by-default toolkit policy и rate limiting остаются без изменений

## Проверка

- локально: `973 passed`
- локально: `line+branch coverage = 100.00%`
- GitHub `CI` должен проходить уже на чистом runner без локально установленного Docker SDK

## Образы GHCR

- `ghcr.io/alekseiseleznev/onec-mcp-universal:1.9.1`
- `ghcr.io/alekseiseleznev/onec-mcp-universal-bsl-graph-lite:1.9.1`
- `ghcr.io/alekseiseleznev/onec-mcp-universal-docker-control:1.9.1`
