"""Large static dashboard docs content extracted from web_ui.py."""

from .config import VERSION as _CFG_VERSION

VERSION = f"v{_CFG_VERSION}"

_DOC_STYLE = """*{margin:0;padding:0;box-sizing:border-box}body{font-family:-apple-system,BlinkMacSystemFont,'Segoe UI',Roboto,sans-serif;background:#0f172a;color:#e2e8f0;padding:32px;max-width:900px;margin:0 auto;line-height:1.7}
h1{color:#f8fafc;margin-bottom:8px;font-size:1.5rem}h2{color:#38bdf8;margin:28px 0 10px;font-size:1.15rem;border-bottom:1px solid #334155;padding-bottom:6px}h3{color:#94a3b8;margin:18px 0 6px;font-size:.95rem}
p,li{color:#94a3b8;font-size:.88rem;margin-bottom:8px}ul,ol{padding-left:24px;margin-bottom:12px}code{background:#334155;padding:2px 6px;border-radius:3px;font-size:.82rem;color:#e2e8f0}
pre{background:#1e293b;padding:12px 16px;border-radius:6px;overflow-x:auto;margin:10px 0;border:1px solid #334155}pre code{background:none;padding:0}
a{color:#38bdf8}.back{display:inline-block;margin-bottom:20px;font-size:.85rem}
table{border-collapse:collapse;width:100%;margin:12px 0;font-size:.85rem}th,td{padding:8px 10px;border:1px solid #334155;text-align:left}th{background:#1e293b;color:#64748b}
.note{background:#1e293b;border-left:3px solid #38bdf8;padding:10px 14px;margin:12px 0;border-radius:0 4px 4px 0}
.warn{background:#1e293b;border-left:3px solid #eab308;padding:10px 14px;margin:12px 0;border-radius:0 4px 4px 0}"""

DOCS_HTML = {
    "ru": r"""<!DOCTYPE html><html lang="ru"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Документация - onec-mcp-universal</title>
<style>""" + _DOC_STYLE + """</style></head><body>
<a class="back" href="/dashboard?lang=ru">&larr; Назад к дашборду</a>
<h1>Документация onec-mcp-universal</h1>
<p>Версия: """ + VERSION + """ | <a href="https://github.com/AlekseiSeleznev/onec-mcp-universal">GitHub</a> | Лицензия: MIT</p>

<h2>Содержание</h2>
<ol>
<li><a href="#overview-ru">Обзор</a> - что это и как устроено</li>
<li><a href="#status-ru">Вкладка "Статус"</a> - базы, бэкенды, отчёты, Docker и текущие метрики</li>
<li><a href="#params-ru">Вкладка "Настройки"</a> - базы, .env и быстрые действия</li>
<li><a href="#epf-ru">Обработка MCPToolkit.epf</a> - подключение, кнопки, безопасность и журнал</li>
<li><a href="#remote-ru">Удалённое подключение</a> - запуск на сервере 1С и ограничения режима</li>
<li><a href="#tools-ru">MCP-инструменты</a> - список инструментов</li>
<li><a href="#api-ru">API-эндпоинты</a> - HTTP API шлюза</li>
<li><a href="#env-ru">Переменные окружения</a> - параметры .env</li>
<li><a href="#diagnostics-ru">Диагностика</a> - что попадает в JSON-отчёт</li>
<li><a href="#troubleshooting-ru">Устранение неполадок</a> - частые сбои и что проверить</li>
</ol>

<!-- ================================================================== -->
<h2 id="overview-ru">1. Обзор</h2>

<h3>Что это такое</h3>
<p><b>onec-mcp-universal</b> - MCP-шлюз для работы с информационными базами 1С:Предприятие из AI-ассистентов и любых MCP-клиентов. MCP (Model Context Protocol) даёт модели способ вызывать внешние инструменты: выполнять запросы, читать метаданные, переходить по BSL-коду и получать служебную диагностику.</p>

<h3>Зачем нужен шлюз</h3>
<p>Без шлюза пришлось бы отдельно подключать данные 1С, навигацию по коду и документацию платформы. Здесь всё собрано за одним адресом: <code>http://localhost:8080/mcp</code>. AI-ассистент подключается один раз и видит доступные MCP-инструменты. Их точное число зависит от настроек, например от включения <code>its_search</code> через <code>NAPARNIK_API_KEY</code>.</p>

<h3>Как работает</h3>
<p>Цепочка взаимодействия выглядит так:</p>
<pre><code>AI-ассистент / MCP-клиент
    |
    | MCP Streamable HTTP
    v
MCP-шлюз (onec-mcp-universal, порт 8080)
    |
    +---> onec-toolkit      - данные 1С (запросы, код, метаданные)
    +---> platform-context  - документация API платформы
    +---> bsl-lsp-bridge    - навигация по BSL-коду
    +---> test-runner       - запуск тестов YaXUnit (опционально)
    |
    v
MCPToolkit.epf (обработка внутри клиента 1С)
    |
    v
Информационная база 1С:Предприятие</code></pre>

<p>Когда AI-ассистент вызывает инструмент, например <code>execute_query</code>, шлюз выбирает нужный бэкенд и передаёт запрос дальше. <code>onec-toolkit</code> отправляет команду в <code>MCPToolkit.epf</code>, запущенную в клиенте 1С. Обработка выполняет действие в информационной базе и возвращает результат обратно через шлюз.</p>

<h3>Маршрутизация по сеансам</h3>
<p>У каждого сеанса AI-ассистента своя активная база. Два окна Codex могут одновременно работать с разными базами, например одно с ERP, другое с ЗУП, и их запросы не смешаются. Все зарегистрированные базы остаются подключёнными. Для переключения используется <code>switch_database</code>. Таймаут простоя сеанса: 8 часов.</p>

<div class="note"><p><b>Одна точка подключения:</b> Вне зависимости от количества подключённых баз и бэкендов, AI-ассистент всегда использует один адрес: <code>http://localhost:8080/mcp</code>.</p></div>

<!-- ================================================================== -->
<h2 id="status-ru">2. Вкладка «Статус»</h2>
<p>«Статус» - основной экран дашборда. Здесь собраны текущие состояния баз, бэкендов, отчётов, кеша, анонимизации и Docker-контейнеров. Вкладка только показывает данные; действия выполняются в «Настройках» или через MCP/API.</p>

<h3>Карточка «Базы данных»</h3>
<p>Таблица всех зарегистрированных баз 1С. В каждой строке показаны:</p>
<table>
<tr><th>Столбец</th><th>Описание</th></tr>
<tr><td><b>Имя</b></td><td>Уникальный идентификатор базы (латинские буквы, цифры, дефис, подчёркивание). Используется в именах Docker-контейнеров: <code>onec-toolkit-{имя}</code> и <code>mcp-lsp-{имя}</code>. Примеры: <code>ERP</code>, <code>ZUP_TEST</code>, <code>buh-main</code>.</td></tr>
<tr><td><b>Подключение</b></td><td>Строка подключения 1С в стандартном формате платформы. Для серверной базы: <code>Srvr=имя_сервера;Ref=имя_базы;</code>. Для файловой: <code>File=/путь/к/базе</code>.</td></tr>
<tr><td><b>Обработка подключена</b></td><td>Статус подключения обработки MCPToolkit.epf. Отображается как цветной кружок без текста:<br>
<span style="color:#22c55e">&#9679;</span> зеленый - обработка запущена в клиенте 1С, канал долгого опроса активен, AI может выполнять запросы.<br>
<span style="color:#eab308">&#9679;</span> желтый - контейнеры запущены, но обработка не подключена. Откройте MCPToolkit.epf и нажмите "Подключиться".</td></tr>
</table>
<p>В отдельной колонке "По умолчанию" отображается зеленый кружок <span style="color:#22c55e">&#9679;</span> у базы, выбранной по умолчанию. Эта база используется для новых сеансов AI, если сеанс не переключился на другую через <code>switch_database</code>. Для остальных баз в этой колонке показана кнопка "По умолчанию", которая позволяет сделать базу активной.</p>
<div class="note"><p><b>Автоподключение:</b> Если пользователь нажимает «Подключиться» в обработке MCPToolkit.epf, а база ещё не зарегистрирована в шлюзе, она автоматически подключается - создаются Docker-контейнеры, база появляется в этой таблице.</p></div>

<h3>Карточка «Профилирование»</h3>
<p>Статистика по вызовам <code>execute_query</code>. Шлюз собирает её с момента запуска и сбрасывает при перезапуске.</p>
<table>
<tr><th>Показатель</th><th>Описание</th></tr>
<tr><td><b>Запросов</b></td><td>Общее количество выполненных запросов с момента запуска шлюза. Считаются все вызовы <code>execute_query</code> ко всем подключённым базам.</td></tr>
<tr><td><b>Среднее</b></td><td>Среднее время выполнения запроса в миллисекундах. Включает время передачи запроса до 1С и получения ответа. Значение больше 1000 мс сигнализирует о необходимости оптимизации запросов.</td></tr>
<tr><td><b>Макс</b></td><td>Максимальное время выполнения одного запроса в миллисекундах. Помогает выявить «выбросы» - единичные тяжёлые запросы.</td></tr>
<tr><td><b>Медл. (&gt;5с)</b></td><td>Количество запросов, выполнявшихся более 5 секунд. Такие запросы считаются медленными и, как правило, требуют оптимизации (добавление индексов, переработка условий, использование временных таблиц).</td></tr>
</table>
<p>Если запросов ещё не было, карточка показывает надпись «Нет запросов».</p>
<p>Кроме дашборда, статистику профилирования можно получить через MCP-инструмент <code>query_stats</code>. Каждый ответ <code>execute_query</code> автоматически включает поле <code>_profiling</code> с длительностью выполнения и подсказками по оптимизации (например: «запрос использует SELECT *», «отсутствует WHERE», «много JOIN»).</p>

<h3>Карточка «Анонимизация»</h3>
<p>Статус маскировки персональных данных в ответах от 1С:</p>
<ul>
<li><span style="color:#22c55e">Включена</span> (зелёный) - все ответы инструментов <code>execute_query</code>, <code>execute_code</code>, <code>get_object_by_link</code>, <code>get_event_log</code> проходят через фильтр анонимизации.</li>
<li><span style="color:#eab308">Выключена</span> (жёлтый) - данные передаются AI без изменений.</li>
</ul>
<p><b>Что маскируется:</b></p>
<ul>
<li><b>ФИО</b> - распознаются паттерны «Фамилия Имя Отчество» и заменяются на стабильные фейковые данные (например, Иванов Иван Иванович всегда превращается в Петров Сергей Александрович)</li>
<li><b>ИНН</b> - 10-значные (юридические лица) и 12-значные (физические лица) номера заменяются на валидные фейковые ИНН</li>
<li><b>СНИЛС</b> - номера формата XXX-XXX-XXX XX</li>
<li><b>Телефоны</b> - российские номера (+7...)</li>
<li><b>Email</b> - адреса электронной почты</li>
<li><b>Названия компаний</b> - юридические лица (ООО, АО, ИП и т.д.)</li>
</ul>
<p><b>Стабильный маппинг:</b> одно и то же исходное значение заменяется одним и тем же фейком в рамках сессии. Связи между объектами при этом сохраняются: если у контрагента были документы, после маскировки они останутся привязаны к тому же замещённому контрагенту.</p>
<p><b>Когда использовать:</b> включайте анонимизацию на рабочих базах с реальными персональными данными. На тестовых базах с синтетикой её обычно можно оставить выключенной.</p>

<h3>Карточка «Кеш метаданных»</h3>
<p>Результаты вызова <code>get_metadata</code> (информация о структуре конфигурации: реквизиты, табличные части, типы) кешируются на стороне шлюза, чтобы не запрашивать одни и те же метаданные у 1С повторно.</p>
<table>
<tr><th>Показатель</th><th>Описание</th></tr>
<tr><td><b>Записей</b></td><td>Количество закешированных объектов метаданных. Каждый уникальный вызов <code>get_metadata</code> с определённым набором параметров создаёт одну запись в кеше.</td></tr>
<tr><td><b>Попадания</b></td><td>Процент запросов, обслуженных из кеша. Значение 80-100% означает, что кеш работает эффективно. Низкий процент может означать, что AI запрашивает каждый раз разные объекты, или что TTL слишком короткий.</td></tr>
<tr><td><b>TTL</b></td><td>Время жизни записи кеша в секундах. По умолчанию - 600 секунд (10 минут). Настраивается через переменную <code>METADATA_CACHE_TTL</code> в файле <code>.env</code>. Значение 0 полностью отключает кеширование.</td></tr>
</table>
<p><b>Когда очищать кеш:</b> После изменения структуры конфигурации 1С (добавление/удаление реквизитов, табличных частей, объектов метаданных). Очистить можно кнопкой на вкладке «Настройки», через MCP-инструмент <code>invalidate_metadata_cache</code>, или дождаться автоматической инвалидации по TTL.</p>

<h3>Карточка «Бэкенды»</h3>
<p>Бэкенд - это отдельный MCP-сервер, который предоставляет набор инструментов определённой категории. Шлюз агрегирует инструменты всех бэкендов и предоставляет их AI-ассистенту как единый набор.</p>
<p>Каждая строка показывает:</p>
<ul>
<li><b>Цветной индикатор</b> - <span style="color:#22c55e">зелёный</span> = бэкенд доступен и отвечает, <span style="color:#ef4444">красный</span> = бэкенд недоступен (контейнер не запущен или произошла ошибка)</li>
<li><b>Имя</b> - идентификатор бэкенда</li>
<li><b>N инстр.</b> - количество инструментов, которые бэкенд предоставляет</li>
<li><b>Бирка «активная»</b> - отображается у бэкендов, привязанных к текущей активной базе</li>
</ul>
<p><b>Статические бэкенды</b> (создаются при запуске шлюза):</p>
<table>
<tr><th>Бэкенд</th><th>Инструменты</th><th>Назначение</th></tr>
<tr><td><code>onec-toolkit</code></td><td>8</td><td>Запросы к БД, выполнение кода, метаданные, журнал регистрации, права доступа, ссылки на объекты. Работает через обработку MCPToolkit.epf.</td></tr>
<tr><td><code>platform-context</code></td><td>5</td><td>Документация API встроенного языка 1С: поиск методов типов, описания конструкторов, список членов объектов.</td></tr>
<tr><td><code>bsl-lsp-bridge</code></td><td>14</td><td>Навигация по BSL-коду: поиск символов, переход к определению, граф вызовов, диагностика ошибок, переименование.</td></tr>
</table>
<p><b>Динамические бэкенды</b> (создаются при подключении каждой базы):</p>
<ul>
<li><code>onec-toolkit-{имя_базы}</code> - персональный бэкенд данных для конкретной базы</li>
<li><code>mcp-lsp-{имя_базы}</code> - персональный сервер языка BSL с индексом конфигурации конкретной базы</li>
</ul>
<p><b>Опциональные бэкенды</b> (подключаются через <code>ENABLED_BACKENDS</code>):</p>
<ul>
<li><code>test-runner</code> - запуск тестов YaXUnit. Добавьте <code>test-runner</code> в <code>ENABLED_BACKENDS</code> и запустите: <code>docker compose --profile test-runner up -d</code>.</li>
</ul>

<h3>Карточка «Отчёты 1С»</h3>
<p>Эта карточка показывает локальную сводку по контуру отчётов. Она не выполняет отчёты, не запускает анализ и не обращается к 1С при открытии страницы. Источник данных - SQLite-каталог <code>/data/report-catalog.sqlite</code> и сохранённые файлы результатов в <code>/data/report-results</code>.</p>
<table>
<tr><th>Показатель</th><th>Описание</th></tr>
<tr><td><b>Последний анализ</b></td><td>Когда для конкретной базы последний раз перестраивался каталог отчётов.</td></tr>
<tr><td><b>Отчётов / Вариантов</b></td><td>Сколько каталогизированных отчётов и вариантов найдено в выгруженных BSL/XML-исходниках.</td></tr>
<tr><td><b>Проверено / Результатов</b></td><td>Сколько каталогизированных отчётов уже имеют последний сохранённый запуск и сколько из них имеют доступный файл результата для повторного чтения.</td></tr>
<tr><td><b>Готово / Нужен ввод / Не поддерживается / Ошибки</b></td><td>Распределение статусов по последнему сохранённому запуску каждого отчёта. Во внутреннем API этим значениям соответствуют <code>done</code>, <code>needs_input</code>, <code>unsupported</code> и <code>error</code>.</td></tr>
</table>
<div class="note"><p><b>Важно:</b> поиск и запуск отчётов не выполняются из дашборда. Они доступны через MCP/API-инструменты <code>find_reports</code>, <code>describe_report</code>, <code>run_report</code> и связанные методы. Кнопка <b>«Обновить статистику»</b> перечитывает эту карточку из локального состояния шлюза; прямых вызовов в 1С здесь нет.</p></div>

<h3>Карточка «Docker-контейнеры»</h3>
<p>Верхняя часть карточки показывает информацию о Docker-демоне: версия Docker, количество CPU, объём RAM, суммарный размер образов и томов.</p>
<p>При открытии дашборда эта карточка загружается в <b>лёгком режиме</b>: тяжёлая статистика Docker не запрашивается автоматически, чтобы страница не подвисала. Вместо этого значения RAM и размеров образов сначала отображаются как <code>н/д</code>. Для явного обновления используйте кнопку <b>«Обновить статистику»</b> рядом с кнопкой обычного обновления страницы.</p>
<p>Ниже - таблица контейнеров проекта с пятью столбцами:</p>
<table>
<tr><th>Столбец</th><th>Описание</th></tr>
<tr><td><b>Контейнер</b></td><td>Имя Docker-контейнера с цветным индикатором состояния</td></tr>
<tr><td><b>Образ</b></td><td>Docker-образ, из которого создан контейнер</td></tr>
<tr><td><b>RAM сейчас</b></td><td>Текущее потребление оперативной памяти контейнером. Это показатель во время работы контейнера, и он меняется со временем.</td></tr>
<tr><td><b>Образ на диске</b></td><td>Размер Docker-образа на диске. Это не потребление RAM, а объём образа в локальном Docker-хранилище.</td></tr>
<tr><td><b>Статус контейнера</b></td><td><code>запущен</code> или <code>остановлен</code></td></tr>
</table>
<div class="note"><p><b>Важно:</b> статус <code>запущен</code> в этой таблице относится именно к Docker-контейнеру. Это не означает, что у соответствующей базы уже подключена обработка EPF. EPF-статус показывается отдельно в карточках баз данных.</p></div>
<p><b>Типы контейнеров:</b></p>
<ul>
<li><code>onec-mcp-gw</code> - MCP-шлюз (этот сервер, порт 8080). Принимает MCP-запросы от AI и маршрутизирует их к бэкендам.</li>
<li><code>onec-mcp-toolkit</code> - статический бэкенд данных (порт 6003). Обрабатывает запросы к 1С через канал долгого опроса с EPF.</li>
<li><code>onec-mcp-platform</code> - бэкенд документации платформы 1С (порт 8081). Содержит структурированную документацию API встроенного языка.</li>
<li><code>onec-toolkit-{имя_базы}</code> - динамический бэкенд данных, создаётся автоматически при подключении каждой новой базы.</li>
<li><code>mcp-lsp-{имя_базы}</code> - BSL Language Server для каждой базы. Индексирует исходники конфигурации и обеспечивает навигацию по коду.</li>
</ul>

<!-- ================================================================== -->
<h2 id="params-ru">3. Вкладка «Настройки»</h2>
<p>На вкладке «Настройки» находятся действия, которые меняют состояние шлюза: подключение и отключение баз, редактирование конфигурации, кеш и анонимизация.</p>

<h3>Управление базами данных</h3>
<p>Карточка «Управление базами данных» содержит таблицу баз с кнопками управления. Имя отключённой базы отображается <span style="text-decoration:line-through;color:#64748b;font-weight:bold">жирным зачёркнутым</span> как на вкладке Статус, так и в Настройках.</p>
<table>
<tr><th>Кнопка</th><th>Описание</th></tr>
<tr><td><b>По умолч.</b></td><td>Устанавливает базу как используемую по умолчанию для новых сеансов AI. Отображается только у баз, которые ещё не являются базой по умолчанию.</td></tr>
<tr><td><b>Изменить</b></td><td>Открывает диалог редактирования строки подключения и пути к проекту. После сохранения необходимо переподключить базу.</td></tr>
<tr><td><b>Переиндексация BSL</b></td><td>Запускает повторную индексацию BSL-исходников в LSP-контейнере базы. Используйте после выгрузки новых исходников или обновления конфигурации.</td></tr>
<tr><td><b style="color:#ef4444">Отключить</b></td><td>Мягкое отключение: останавливает Docker-контейнеры базы, удаляет активные бэкенды, но <b>оставляет запись в реестре</b>. После отключения кнопка меняется на «Подключить». Данные 1С, физические BSL-файлы, граф и служебное состояние базы не удаляются.</td></tr>
<tr><td><b>Подключить</b></td><td>Повторное подключение отключённой базы: запускает контейнеры заново из сохранённой конфигурации. Страница автоматически обновляется, когда контейнеры готовы. MCPToolkit.epf в 1С переподключится самостоятельно.</td></tr>
<tr><td><b style="color:#ef4444">Удалить</b></td><td>Полное удаление: останавливает контейнеры, очищает контур выполнения, служебное состояние и граф базы и удаляет запись из реестра. Операция необратима — для повторного подключения потребуется заново заполнить форму «Добавить базу». Данные 1С и физические BSL-файлы на диске не затрагиваются.</td></tr>
</table>

<h3>Добавление базы</h3>
<p>Внизу карточки находится форма «Добавить базу»:</p>
<table>
<tr><th>Поле</th><th>Описание</th><th>Пример</th></tr>
<tr><td><b>Имя базы</b></td><td>Уникальный идентификатор. Допустимы латинские буквы, цифры, дефис и подчёркивание. Максимум 63 символа.</td><td><code>MY_INFOBASE</code></td></tr>
<tr><td><b>Строка подключения</b></td><td>Стандартная строка подключения платформы 1С. Серверная: <code>Srvr=имя_сервера;Ref=имя_базы;</code>. Файловая: <code>File=/путь/к/базе</code>. С авторизацией: <code>Srvr=srv;Ref=db;Usr=admin;Pwd=пароль;</code></td><td><code>Srvr=localhost;Ref=MY_INFOBASE;</code></td></tr>
<tr><td><b>Путь к проекту</b></td><td>Абсолютный путь на хосте, куда будут выгружены BSL-исходники конфигурации. Этот каталог монтируется в LSP-контейнер для навигации по коду.</td><td><code>/home/user/projects/MY_INFOBASE</code></td></tr>
</table>
<p>После нажатия кнопки «Подключить» шлюз создаст два Docker-контейнера для базы и зарегистрирует её. Далее откройте MCPToolkit.epf в клиенте 1С и нажмите «Подключиться».</p>

<h3>Три способа подключения базы</h3>
<p>Базу 1С можно подключить тремя способами:</p>
<ol>
<li><b>Из обработки MCPToolkit.epf</b> (рекомендуемый) - откройте обработку в клиенте 1С:Предприятие, нажмите «Подключиться». Обработка автоматически определит имя базы и сервер, зарегистрирует базу в шлюзе и создаст Docker-контейнеры. Никакой ручной настройки не требуется.</li>
<li><b>Из дашборда</b> - перейдите на вкладку «Настройки», заполните форму «Добавить базу» и нажмите «Подключить». После этого откройте MCPToolkit.epf в 1С и нажмите «Подключиться» для установки канала долгого опроса.</li>
<li><b>Через AI-ассистент</b> - напишите в чате с AI на естественном языке:
<pre><code>Подключи базу MY_INFOBASE, строка подключения Srvr=localhost;Ref=MY_INFOBASE;, папка /home/user/projects/MY_INFOBASE</code></pre>
AI вызовет инструмент <code>connect_database</code> с указанными параметрами. После этого откройте MCPToolkit.epf в 1С и нажмите «Подключиться».</li>
</ol>

<h3>Конфигурация шлюза</h3>
<p>Вторая карточка на вкладке «Настройки» показывает таблицу текущих значений всех переменных окружения шлюза (файл <code>.env</code>). В верхней части расположены кнопки действий (очистка кеша, анонимизация) и кнопка «Редактировать».</p>
<p><b>Процесс редактирования:</b></p>
<ol>
<li>Нажмите кнопку <b>«Редактировать»</b> - таблица заменится на текстовый редактор с полным содержимым файла <code>.env</code>.</li>
<li>Внесите изменения. Формат: <code>ПЕРЕМЕННАЯ=значение</code>, по одной переменной на строку. Строки, начинающиеся с <code>#</code>, являются комментариями.</li>
<li>Нажмите <b>«Сохранить»</b> - файл сохранится, и шлюз <b>автоматически перезапустится</b> через несколько секунд. Все текущие MCP-сессии будут разорваны (AI-ассистент переподключится автоматически).</li>
<li>Или нажмите <b>«Отмена»</b> - редактор закроется без сохранения.</li>
</ol>
<div class="note"><p><b>Монтирование:</b> gateway читает <code>.env</code> как <code>./.env:/data/.env:ro</code>, а sidecar <code>docker-control</code> получает <code>./.env:/data/.env:rw</code> только для записи изменений из дашборда обратно на хост. Работает на Linux и Windows (через <code>docker-compose.windows.yml</code>).</p></div>

<h3>Папка выгрузки BSL</h3>
<p>Эта карточка задаёт корневую папку на хосте для выгруженных BSL-исходников. Для каждой базы внутри неё создаётся отдельный подкаталог.</p>
<p>Кнопка <b>«Обзор...»</b> открывает стандартный системный диалог выбора папки на самом хосте. Этот диалог запускает <code>export-host-service.py</code>, поэтому для его работы должен быть доступен <code>EXPORT_HOST_URL</code> и сам сервис выгрузки.</p>
<div class="note"><p><b>Важно:</b> после сохранения новая папка применяется без перезапуска шлюза. Работающие базы автоматически переводятся на новый корень выгрузки, а контейнер графа пересоздаётся, чтобы подхватить новое монтирование.</p></div>

<h3>Движок отчётов 1С</h3>
<p>Карточка параметров отчётного контура задаёт значения по умолчанию для MCP/API-инструментов работы с отчётами. Дашборд не запускает отчёты: он только сохраняет настройки, которые затем использует <code>run_report</code> и пакетная проверка.</p>
<table>
<tr><th>Параметр</th><th>Описание</th></tr>
<tr><td><b>Автоанализ после подключения, выгрузки и переиндексации BSL</b></td><td>Если включён, шлюз автоматически перестраивает каталог отчётов после подключения базы, успешной выгрузки исходников и явной переиндексации BSL.</td></tr>
<tr><td><b>Использовать API-движок</b></td><td>Разрешает быстрый запуск без браузера через контур <code>toolkit</code>/<code>EPF</code>. Это основной путь для отчётов.</td></tr>
<tr><td><b>Использовать UI-движок</b></td><td>Разрешает запуск через веб-клиент 1С. Такой запуск открывает отчёт как пользователь, заполняет параметры, формирует отчёт и сохраняет временный файл для разбора.</td></tr>
<tr><td><b>Переходить на UI при техническом ограничении API</b></td><td>В режиме <code>runner=auto</code> разрешает попробовать веб-интерфейс, если API-контур не смог получить корректную табличную часть.</td></tr>
<tr><td><b>Формат экспорта UI</b></td><td>Формат временного файла для UI-движка: <code>XLSX</code> или <code>HTML</code>. После успешного разбора файл удаляется.</td></tr>
<tr><td><b>Хранить файлы ошибок UI</b></td><td>Оставляет временные файлы только для диагностики неудачного запуска через веб-интерфейс.</td></tr>
<tr><td><b>Строк при запуске / Таймаут запуска</b></td><td>Значения по умолчанию для <code>run_report</code>, если вызывающий клиент не передал собственные <code>max_rows</code> и <code>timeout_seconds</code>.</td></tr>
<tr><td><b>Строк при проверке / Таймаут проверки</b></td><td>Значения по умолчанию для <code>validate_all_reports</code>. Они ограничивают объём и длительность массового прогона каталога.</td></tr>
</table>
<p>Сохранение этой карточки не перезапускает шлюз: новые значения применяются сразу и одновременно записываются в <code>.env</code>.</p>

<h3>Действия</h3>
<p>Кнопки быстрых действий расположены над таблицей конфигурации:</p>
<table>
<tr><th>Действие</th><th>Описание</th><th>Когда использовать</th></tr>
<tr><td><b>Очистить кеш</b></td><td>Удаляет все закешированные результаты <code>get_metadata</code>. Следующие запросы метаданных обратятся напрямую к 1С.</td><td>После изменения структуры конфигурации: добавления/удаления реквизитов, табличных частей, объектов метаданных. Также полезно, если AI получает устаревшую информацию о структуре.</td></tr>
<tr><td><b>Анонимизация вкл/выкл</b></td><td>Переключает маскировку персональных данных в ответах инструментов. Не требует перезапуска шлюза, вступает в силу мгновенно.</td><td>Включайте при работе с рабочими базами, содержащими реальные ФИО, ИНН, телефоны. Выключайте при работе с тестовыми базами или когда маскировка мешает анализу данных.</td></tr>
</table>

<!-- ================================================================== -->
<h2 id="epf-ru">4. Обработка MCPToolkit.epf</h2>
<p>Внешняя обработка <code>MCPToolkit.epf</code> - это ключевой компонент, работающий на стороне 1С. Она запускается в клиенте 1С:Предприятие и выступает посредником между MCP-шлюзом и информационной базой. Без неё невозможны операции с данными 1С (запросы, выполнение кода, чтение метаданных).</p>
<p>Файл обработки находится в каталоге проекта: <code>1c/MCPToolkit/build/MCPToolkit.epf</code>.</p>

<h3>Поля интерфейса</h3>
<table>
<tr><th>Поле</th><th>Описание</th><th>Заполнение</th></tr>
<tr><td><b>Адрес шлюза</b></td><td>URL MCP-шлюза. Используется для регистрации базы и отправки результатов.</td><td>По умолчанию <code>http://localhost:8080</code>. Измените, если шлюз работает на другом хосте или порту. Если включён <code>GATEWAY_API_TOKEN</code>, используйте формат <code>http://host:8080#token=&lt;секрет&gt;</code>.</td></tr>
<tr><td><b>Информационная база: имя</b></td><td>Идентификатор текущей информационной базы. Определяет, к какому набору Docker-контейнеров будет привязана обработка.</td><td>Заполняется автоматически из строки подключения текущей ИБ. Только чтение.</td></tr>
<tr><td><b>Информационная база: сервер</b></td><td>Имя сервера 1С или путь к файловой базе.</td><td>Определяется автоматически. Только чтение.</td></tr>
<tr><td><b>Информационная база: пользователь</b></td><td>Имя пользователя текущего сеанса 1С.</td><td>Определяется автоматически. Только чтение.</td></tr>
<tr><td><b>Информационная база: пароль</b></td><td>Пароль для выгрузки BSL-исходников через DESIGNER. Необходим только для операции «Выгрузить BSL».</td><td>Вводится вручную. Не сохраняется между сеансами.</td></tr>
</table>

<h3>Кнопки</h3>
<table>
<tr><th>Кнопка</th><th>Действие</th></tr>
<tr><td><b>Подключиться</b></td><td>Выполняет два действия:<br>1. <b>Регистрация</b> - отправляет данные базы (имя, строку подключения, текущий <code>channel</code> экземпляра формы) на шлюз через <code>/api/register</code>. Если база ещё не подключена, шлюз автоматически создаёт Docker-контейнеры.<br>2. <b>Канал долгого опроса</b> - устанавливает постоянное соединение с toolkit-бэкендом. Обработка начинает «слушать» входящие команды от AI и выполнять их в контексте текущей информационной базы.</td></tr>
<tr><td><b>Отключиться</b></td><td>Прекращает канал долгого опроса. Обработка перестаёт принимать команды. Docker-контейнеры продолжают работать - база остаётся зарегистрированной, но статус EPF меняется на «Не подкл.».</td></tr>
<tr><td><b>Выгрузить BSL</b></td><td>Запускает выгрузку исходников конфигурации 1С в BSL-файлы для навигации по коду. Использует <code>1cv8 DESIGNER /DumpConfigToFiles</code> с автоматическим определением версии платформы. При несовпадении версий клиента и сервера шлюз автоматически находит и использует подходящий бинарник. На крупных конфигурациях (ERP - ~18 000 модулей, ЗУП - ~12 000 модулей) индексация занимает 3-5 минут. После завершения BSL Language Server автоматически переиндексирует файлы.</td></tr>
</table>

<h3>Автоматически разрешать операции</h3>
<p>Три чекбокса управляют автоматическим подтверждением потенциально опасных операций, когда AI выполняет произвольный код через инструмент <code>execute_code</code>. При каждом свежем открытии обработки все три начинаются в состоянии <code>выключено</code> и действуют только для текущего открытого экземпляра формы:</p>
<table>
<tr><th>Чекбокс</th><th>Описание</th><th>Рекомендация</th></tr>
<tr><td><b>Записать объект</b></td><td>Если включён - AI может выполнять запись данных (<code>Объект.Записать()</code>, <code>Документ.Записать(РежимЗаписиДокумента.Проведение)</code>) без ручного подтверждения оператором. Если выключён - каждая попытка записи вызывает диалоговое окно в клиенте 1С с вопросом «Разрешить запись?».</td><td>На рабочих базах - <b>выключен</b>. На тестовых базах - на усмотрение пользователя.</td></tr>
<tr><td><b>Привилегированный режим</b></td><td>Если включён - AI может устанавливать привилегированный режим (<code>УстановитьПривилегированныйРежим(Истина)</code>) без подтверждения. Привилегированный режим отключает проверку прав доступа в 1С - код выполняется от имени пользователя с полными правами.</td><td>На рабочих базах - <b>выключен</b>. Включать только при необходимости и с пониманием рисков.</td></tr>
<tr><td><b>Все опасные операции без предупреждения</b></td><td>Глобально отключает предупреждения для потенциально опасных операций. При включении форма показывает отдельный вопрос-подтверждение с перечислением действий: запись, удаление, файловые операции, COM/add-in вызовы, привилегированный и монопольный режим.</td><td>На рабочих базах - <b>выключен</b>. Включать только для контролируемых тестовых сценариев.</td></tr>
</table>
<div class="warn"><p><b>Безопасность:</b> На рабочих базах с реальными данными рекомендуется оставить все три чекбокса выключенными. Каждая потенциально опасная операция будет требовать ручного подтверждения оператором в клиенте 1С. Это предотвращает случайное удаление или модификацию данных.</p></div>

<h3>Статус и ссылки</h3>
<p>В нижней части формы отображается текущий статус подключения (Подключено / Отключено) и ссылки:</p>
<ul>
<li><b>Дашборд</b> - открывает веб-интерфейс шлюза (<code>/dashboard</code>) в браузере</li>
<li><b>GitHub</b> - ссылка на репозиторий проекта</li>
</ul>

<h3>Журнал событий</h3>
<p>Единый журнал в нижней части формы отображает все события в хронологическом порядке:</p>
<ul>
<li>Успешные подключения и отключения</li>
<li>Входящие команды от AI (имя инструмента, параметры)</li>
<li>Результаты выполнения команд</li>
<li>Ошибки (сетевые, исключения 1С, таймауты)</li>
<li>Информация о выгрузке BSL</li>
<li>Предупреждения об удалённом подключении (если шлюз на другом сервере)</li>
</ul>
<p>Журнал полезен для отладки. Если AI-ассистент получает ошибку, в журнале будет видно, что именно произошло на стороне 1С.</p>

<h3>Вкладка "Анонимизация"</h3>
<p>Дополнительная вкладка в обработке позволяет настроить правила <b>точной анонимизации</b> на стороне EPF: regex-паттерны, словари замен, списки исключений. Эта анонимизация работает <b>независимо</b> от серверной анонимизации шлюза и может использоваться совместно с ней или отдельно.</p>

<h3>Автономный режим (резервный вариант)</h3>
<p>Если шлюз недоступен (сеть не работает, контейнеры остановлены, сервер выключен), обработка MCPToolkit.epf переключается на <b>прямое подключение</b> к бэкенду onec-toolkit. В этом режиме обработка работает напрямую без шлюза.</p>
<p><b>Как это работает:</b></p>
<ol>
<li>При нажатии "Подключиться" обработка проверяет доступность шлюза (таймаут 5 секунд).</li>
<li>Если шлюз доступен, первый <code>/api/register</code> на холодном старте может ждать до 240 секунд: за это время шлюз успевает поднять toolkit/LSP-контейнеры и вернуть привязанный к форме <code>channel</code> backend.</li>
<li>Если шлюз отвечает, обработка работает через шлюз как обычно.</li>
<li>Если шлюз не отвечает, обработка подключается напрямую к toolkit-бэкенду.</li>
<li>В журнале событий появится предупреждение: "Шлюз недоступен, используется прямое подключение к toolkit".</li>
</ol>
<p><b>Ограничения автономного режима:</b> Без шлюза недоступны навигация по BSL, документация платформы, анонимизация, кеширование метаданных и профилирование. Работают только инструменты onec-toolkit: запросы к БД, выполнение кода, метаданные, журнал регистрации, права доступа.</p>

<!-- ================================================================== -->
<h2 id="remote-ru">5. Удалённое подключение</h2>
<p>Если сервер 1С находится на удалённой машине и пользователи подключаются к нему через тонкий клиент или веб-клиент, шлюз можно развернуть прямо на этом сервере. AI-ассистент на рабочей станции разработчика подключится к шлюзу по сети.</p>

<h3>Когда это нужно</h3>
<p>Типичная ситуация: сервер 1С стоит в серверной или в облаке. Пользователи работают через тонкий клиент. AI-ассистент или MCP-клиент запущен на рабочей станции разработчика. Платформа 1С не установлена на рабочей станции - только тонкий клиент. В этом случае шлюз нужно поставить на сервер 1С, а клиент подключится к нему по сети через порт 8080.</p>

<h3>Развёртывание на сервере 1С</h3>
<ol>
<li>Установите Docker и Docker Compose на сервере 1С.</li>
<li>Клонируйте проект: <code>git clone https://github.com/AlekseiSeleznev/onec-mcp-universal.git</code></li>
<li>Создайте <code>.env</code> файл: <code>cp .env.example .env</code></li>
<li>Укажите путь к платформе: <code>PLATFORM_PATH=/opt/1cv8/x86_64/8.3.27.2074</code></li>
<li>Запустите контейнеры: <code>docker compose up -d</code></li>
<li>Откройте порт 8080 в файрволе сервера: <code>sudo ufw allow 8080/tcp</code></li>
</ol>
<p>Проверьте доступность с рабочей станции: <code>curl http://server-erp:8080/health</code></p>

<h3>Подключение AI-ассистента</h3>
<p>На рабочей станции разработчика укажите адрес сервера вместо localhost:</p>
<pre><code># Codex
codex mcp add onec --url http://server-erp:8080/mcp

# Другие MCP-клиенты
# URL: http://server-erp:8080/mcp</code></pre>
<p>Здесь <code>server-erp</code> - имя или IP-адрес сервера 1С (например, <code>192.168.1.100</code>).</p>
<p>Дашборд тоже будет доступен удалённо: <code>http://server-erp:8080/dashboard</code></p>

<h3>Подключение EPF</h3>
<p>Обработку MCPToolkit.epf нужно открыть в тонком клиенте 1С на сервере. В поле "Адрес шлюза" укажите <code>http://localhost:8080</code>, так как шлюз и обработка работают на одной машине. Нажмите "Подключиться".</p>
<p>Если включён <code>GATEWAY_API_TOKEN</code>, укажите адрес в формате <code>http://localhost:8080#token=&lt;секрет&gt;</code>. Токен будет отправлен как <code>Authorization: Bearer ...</code> только в mutating API вызовах.</p>
<p>Если 1С-клиент запущен на рабочей станции (не на сервере), в поле "Адрес шлюза" укажите адрес сервера: <code>http://server-erp:8080</code> (или <code>http://server-erp:8080#token=&lt;секрет&gt;</code> для защищённого API).</p>

<h3>Что работает при удалённом подключении</h3>
<p>Большинство инструментов работают без ограничений. Всё, что проходит через EPF (данные, метаданные, журнал), работает одинаково - локально и удалённо. BSL-навигация требует отдельной настройки.</p>
<table>
<tr><th>Категория</th><th>Инструменты</th><th>Статус</th><th>Примечание</th></tr>
<tr><td>Запросы к данным</td><td><code>execute_query</code>, <code>execute_code</code></td><td><span style="color:#22c55e">Работает</span></td><td>Все запросы выполняются через EPF на сервере</td></tr>
<tr><td>Метаданные</td><td><code>get_metadata</code>, <code>get_access_rights</code></td><td><span style="color:#22c55e">Работает</span></td><td>Структура конфигурации и роли через EPF</td></tr>
<tr><td>Журнал регистрации</td><td><code>get_event_log</code></td><td><span style="color:#22c55e">Работает</span></td><td>Чтение и фильтрация журнала через EPF</td></tr>
<tr><td>Ссылки и поиск</td><td><code>get_object_by_link</code>, <code>get_link_of_object</code>, <code>find_references_to_object</code></td><td><span style="color:#22c55e">Работает</span></td><td>Навигационные ссылки и поиск использований</td></tr>
<tr><td>Документация</td><td><code>search</code>, <code>info</code>, <code>getMembers</code>, <code>getMember</code>, <code>getConstructors</code></td><td><span style="color:#22c55e">Работает</span></td><td>Документация хранится в контейнере на сервере</td></tr>
<tr><td>Валидация запросов</td><td><code>validate_query</code></td><td><span style="color:#22c55e">Работает</span></td><td>Статическая + серверная проверка</td></tr>
<tr><td>Анонимизация</td><td><code>enable_anonymization</code>, <code>disable_anonymization</code></td><td><span style="color:#22c55e">Работает</span></td><td>Маскировка ПД на стороне шлюза</td></tr>
<tr><td>Поиск по ИТС</td><td><code>its_search</code></td><td><span style="color:#22c55e">Работает</span></td><td>API 1С:Напарника из контейнера на сервере</td></tr>
<tr><td>Управление</td><td><code>connect_database</code>, <code>switch_database</code>, <code>list_databases</code> и др.</td><td><span style="color:#22c55e">Работает</span></td><td>Управление базами через шлюз</td></tr>
<tr><td>BSL-навигация</td><td><code>definition</code>, <code>symbol_explore</code>, <code>call_graph</code>, <code>hover</code> и др.</td><td><span style="color:#eab308">Требует настройки</span></td><td>Нужно выгрузить BSL-файлы на сервер</td></tr>
<tr><td>Выгрузка BSL</td><td><code>export_bsl_sources</code></td><td><span style="color:#22c55e">Работает</span></td><td>1cv8 DESIGNER с автоопределением версии платформы</td></tr>
</table>

<h3>Поведение обработки MCPToolkit.epf</h3>
<p>При подключении обработка определяет, работает ли шлюз локально или удалённо. Если адрес шлюза отличается от <code>localhost</code> или <code>127.0.0.1</code>, обработка:</p>
<ul>
<li>Показывает предупреждение в журнале: "Удалённый шлюз, некоторые функции могут быть ограничены"</li>
<li>Кнопка "Выгрузить BSL" работает только если на сервере запущен сервис выгрузки (<code>export-host-service.py</code>)</li>
<li>Все остальные функции (подключение, отключение, выполнение команд) работают без ограничений</li>
</ul>
<p>Если шлюз развёрнут на том же сервере, где работает 1С, в поле "Адрес шлюза" оставьте <code>http://localhost:8080</code>. В этом случае обработка работает в полном режиме.</p>
<p>При работе через веб-клиент или удаленный тонкий клиент обработка автоматически определяет удаленное подключение и выводит в журнал:</p>
<pre><code>Удаленное подключение (веб/тонкий клиент).
Доступно: запросы, код, метаданные, журнал, права.
Недоступно: BSL навигация, выгрузка BSL.
Для полной работы: docker compose на сервере 1С.</code></pre>

<h3>Порты для удаленного доступа</h3>
<p>Минимальный набор портов, которые нужно открыть на сервере 1С:</p>
<table>
<tr><th>Порт</th><th>Сервис</th><th>Назначение</th></tr>
<tr><td><code>8080</code></td><td>MCP-шлюз</td><td>AI-ассистент подключается к <code>/mcp</code>, дашборд на <code>/dashboard</code></td></tr>
<tr><td><code>443</code> или <code>80</code></td><td>Веб-публикация 1С</td><td>Тонкий клиент в браузере (обычно уже открыт)</td></tr>
<tr><td><code>3389</code></td><td>RDP (опционально)</td><td>Удаленный рабочий стол для доступа к Конфигуратору</td></tr>
</table>
<p>Если нужен прямой доступ к Конфигуратору без RDP:</p>
<table>
<tr><th>Порт</th><th>Назначение</th></tr>
<tr><td><code>1540-1541</code></td><td>Агент и менеджер кластера 1С</td></tr>
<tr><td><code>1560-1591</code></td><td>Рабочие процессы кластера 1С</td></tr>
</table>
<p>Все остальные порты (6003, 6100+, 8081) работают внутри сервера через localhost и не требуют открытия наружу.</p>

<h3>Настройка навигации по BSL на удалённом сервере</h3>
<p>Навигация по BSL (переход к определению, поиск символов, граф вызовов) работает только при наличии выгруженных BSL-файлов на сервере. Без них инструменты <code>definition</code>, <code>symbol_explore</code>, <code>call_graph</code> вернут пустые результаты. Есть два способа получить BSL-файлы:</p>

<p><b>Способ 1. Выгрузка через EPF (автоматический):</b></p>
<ol>
<li>Запустите сервис выгрузки на сервере: <code>python3 tools/export-host-service.py --port 8082</code></li>
<li>Убедитесь, что в <code>.env</code> указан: <code>EXPORT_HOST_URL=http://localhost:8082</code></li>
<li>Если host-service недоступен и вы осознанно используете HASP/CI: установите <code>ALLOW_CONTAINER_DESIGNER_EXPORT=true</code>. По умолчанию контейнерная выгрузка отключена.</li>
<li>Откройте MCPToolkit.epf в 1С, нажмите "Выгрузить BSL"</li>
<li>Дождитесь завершения (3-5 минут для ERP, 1-2 минуты для небольших конфигураций)</li>
</ol>

<p><b>Способ 2. Ручной экспорт и копирование:</b></p>
<ol>
<li>Выгрузите конфигурацию в XML на любой машине с платформой 1С: <code>ibcmd /S server-erp/ERP_Test /DumpConfigToFiles /home/user/export</code></li>
<li>Скопируйте файлы на сервер: <code>scp -r /home/user/export user@server-erp:/home/user/1c-projects/ERP_Test</code></li>
<li>Попросите AI-ассистент переиндексировать: "Переиндексируй BSL-файлы"</li>
</ol>
<p>После выгрузки AI получит полный доступ к навигации: переход к определению, граф вызовов, поиск символов, диагностика ошибок, переименование.</p>

<h3>Схема удалённого подключения</h3>
<pre><code>Рабочая станция                     Сервер 1С (server-erp)
+-----------------+                  +-----------------------------+
| Codex / другой  |   HTTP :8080     | Docker                      |
| MCP-клиент      | ---------------> | onec-mcp-gw (шлюз)          |
|                 |   /mcp           | onec-toolkit (бэкенд)       |
+-----------------+                  | platform-context            |
                                     | mcp-lsp (навигация BSL)     |
                                     +-----------------------------+
                                              ^
                                              | localhost
                                     +-----------------------------+
                                     | Тонкий клиент 1С            |
                                     | MCPToolkit.epf              |
                                     +-----------------------------+
                                              |
                                     +-----------------------------+
                                     | Сервер 1С:Предприятие       |
                                     | Информационная база         |
                                     +-----------------------------+</code></pre>

<div class="warn"><p><b>Безопасность:</b> Порт 8080 открывает доступ ко всем инструментам шлюза, включая выполнение кода и запросы к БД. Ограничьте доступ по IP-адресам через файрвол. Не открывайте порт в интернет без дополнительной защиты (VPN, SSH-туннель). Пример ограничения: <code>sudo ufw allow from 192.168.1.0/24 to any port 8080</code></p></div>

<!-- ================================================================== -->
<h2 id="tools-ru">6. MCP-инструменты</h2>
<p>Шлюз предоставляет AI-ассистенту полный набор инструментов, объединённых из всех бэкендов. Ниже приведён полный список MCP-инструментов по категориям. Точное число зависит от включения <code>its_search</code> (требуется <code>NAPARNIK_API_KEY</code>).</p>

<h3>Данные 1С (через onec-toolkit, 8 инструментов)</h3>
<table>
<tr><th>Инструмент</th><th>Описание</th></tr>
<tr><td><code>execute_query</code></td><td>Выполнение запроса к БД на встроенном языке запросов 1С. Поддерживает параметры (<code>&amp;Параметр</code>), лимиты, вложенные запросы. Автоматически профилируется и анонимизируется (если включено).</td></tr>
<tr><td><code>execute_code</code></td><td>Выполнение произвольного кода на встроенном языке 1С. Поддерживает <code>execution_context=\"server\"</code> для изменения данных и чтения метаданных, а <code>execution_context=\"client\"</code> - для реквизитов формы EPF и проверок флагов безопасности. Возврат идёт через переменную <code>Результат</code>.</td></tr>
<tr><td><code>get_metadata</code></td><td>Получение структуры объекта метаданных конфигурации: реквизиты, табличные части, типы полей, длина строк, и т.д. Результат кешируется.</td></tr>
<tr><td><code>get_event_log</code></td><td>Чтение журнала регистрации 1С с фильтрацией по дате, событию, пользователю, уровню важности.</td></tr>
<tr><td><code>get_object_by_link</code></td><td>Получение данных объекта по навигационной ссылке 1С (формат <code>e1cib/...</code>).</td></tr>
<tr><td><code>get_link_of_object</code></td><td>Генерация навигационной ссылки на объект. Текущий API ожидает аргумент <code>object_description</code> с описанием ссылки объекта.</td></tr>
<tr><td><code>find_references_to_object</code></td><td>Поиск всех мест использования объекта. Текущий API ожидает <code>target_object_description</code> и <code>search_scope</code>.</td></tr>
<tr><td><code>get_access_rights</code></td><td>Анализ ролей и прав доступа на объекты метаданных. Показывает, какие роли имеют доступ и какие операции разрешены.</td></tr>
</table>

<h3>Отчёты 1С по пользовательскому названию (15 инструментов)</h3>
<table>
<tr><th>Инструмент</th><th>Описание</th></tr>
<tr><td><code>analyze_reports</code></td><td>Строит SQLite-каталог отчётов для конкретной базы по выгруженным BSL/XML-исходникам.</td></tr>
<tr><td><code>enrich_report_docs</code></td><td>Загружает и кеширует описание отчёта из ИТС/1С:Напарника для улучшения поиска по бухгалтерским формулировкам.</td></tr>
<tr><td><code>find_reports</code></td><td>Ищет отчёт по названию из интерфейса 1С, синониму, варианту или техническому имени.</td></tr>
<tr><td><code>list_reports</code></td><td>Показывает отчёты, найденные для указанной базы. Аргумент <code>database</code> обязателен.</td></tr>
<tr><td><code>describe_report</code></td><td>Показывает технический отчёт, вариант, параметры, стратегию запуска и контракт структуры результата.</td></tr>
<tr><td><code>get_report_runner_policy</code></td><td>Возвращает сохранённую политику выбора API-движка или UI-движка для отчёта или варианта.</td></tr>
<tr><td><code>set_report_runner_policy</code></td><td>Сохраняет предпочтительный движок: <code>api</code>, <code>ui</code> или <code>auto</code>.</td></tr>
<tr><td><code>get_report_ui_strategy</code></td><td>Возвращает сценарий открытия отчёта, заполнения полей, формирования и экспорта через веб-клиент 1С.</td></tr>
<tr><td><code>set_report_ui_strategy</code></td><td>Сохраняет точный сценарий веб-запуска для отчёта или варианта.</td></tr>
<tr><td><code>run_report</code></td><td>Запускает отчёт по пользовательскому названию. Параметр <code>runner</code> выбирает <code>api</code>, <code>ui</code> или <code>auto</code>.</td></tr>
<tr><td><code>validate_all_reports</code></td><td>Быстро проверяет каталог отчётов и записывает статусы запусков.</td></tr>
<tr><td><code>validate_report_contracts</code></td><td>Запускает тяжёлую проверку вариантов отчётов: сравнивает ожидаемую и фактическую структуру результата и сохраняет кампанию.</td></tr>
<tr><td><code>get_report_result</code></td><td>Возвращает страницу сохранённого результата большого отчёта по <code>run_id</code>.</td></tr>
<tr><td><code>get_report_validation_campaign</code></td><td>Возвращает сохранённую кампанию тяжёлой контрактной проверки.</td></tr>
<tr><td><code>explain_report_strategy</code></td><td>Объясняет, почему выбрана конкретная стратегия запуска, и показывает последнюю контрактную диагностику.</td></tr>
</table>
<p><b>Архитектура:</b> инструменты работы с отчётами состоят из локального SQLite-каталога, анализатора BSL/XML/СКД, контракта результата и двух независимых контуров запуска. API-движок работает через <code>toolkit</code>/<code>EPF</code> без браузера. UI-движок работает через веб-клиент 1С: открывает отчёт, заполняет параметры, формирует его, сохраняет временный <code>XLSX</code> или <code>HTML</code>, разбирает файл и удаляет его после успешного чтения.</p>
<p><b>Как получать данные:</b> обычно поток такой: <code>find_reports</code> для поиска по пользовательскому названию, <code>describe_report</code> для просмотра параметров, вариантов и контракта результата, затем <code>run_report</code> с явным <code>database</code>, <code>runner=auto</code> и при необходимости с <code>period</code>, <code>filters</code>, <code>params</code> и <code>context</code>. Если ответ большой, его дочитывают через <code>get_report_result</code>. Если модуль запуска возвращает <code>needs_input</code>, это означает формализованный запрос недостающих параметров или контекста выполнения для повторного вызова, а не скрытую ошибку.</p>
<p><b>Контракты результата:</b> <code>describe_report</code> возвращает <code>output_contract</code>. Он описывает ожидаемую форму результата: строки, артефакт, колонки, группировки, итоги и детальную часть. После запуска шлюз строит наблюдаемую структуру и сравнивает её с контрактом. Если API не вернул корректную табличную часть, а UI-движок включён и переход на UI разрешён, режим <code>runner=auto</code> может перейти на веб-интерфейс.</p>
<p><b>Политики и сценарии:</b> <code>set_report_runner_policy</code> задаёт предпочтительный движок для отчёта или варианта. <code>set_report_ui_strategy</code> сохраняет точный сценарий веб-интерфейса: как открыть отчёт, какие поля заполнить, какую кнопку нажать и в каком формате выгрузить результат.</p>
<p><b>Обновление каталога:</b> каталог можно перестроить вручную через <code>analyze_reports</code>. Он также может обновляться после операций экспорта и переиндексации, когда шлюз видит новые выгруженные исходники.</p>
<div class="note"><p><b>Пример:</b> <code>run_report(database="MY_INFOBASE", title="Расчетный листок", period={"from":"2025-12-01","to":"2025-12-31"}, filters={"Организация":"Основная"}, params={"ВариантОтчета":"Основной"}, runner="auto")</code>. Инструменты работы с отчётами всегда требуют явный <code>database</code>, чтобы несколько подключённых баз не смешивались. Для запросов вроде «расчетка сотрудника» можно предварительно вызвать <code>enrich_report_docs</code>; базовый каталог работает и без Напарника.</p></div>

<h3>Документация платформы (через platform-context, 5 инструментов)</h3>
<table>
<tr><th>Инструмент</th><th>Описание</th></tr>
<tr><td><code>info</code></td><td>Общая информация о типе платформы 1С: описание, назначение, базовый тип.</td></tr>
<tr><td><code>getMembers</code></td><td>Список всех свойств и методов типа (например, все методы объекта <code>Запрос</code>).</td></tr>
<tr><td><code>getMember</code></td><td>Детальное описание конкретного метода или свойства типа: параметры, типы возврата, описание.</td></tr>
<tr><td><code>getConstructors</code></td><td>Описание конструкторов типа (варианты создания через <code>Новый</code>).</td></tr>
<tr><td><code>search</code></td><td>Полнотекстовый поиск по документации платформы: типы, методы, свойства.</td></tr>
</table>
<p><b>Важно:</b> текущая схема `platform-context` строже, чем раньше: <code>search</code> требует <code>query</code>, <code>type</code> и <code>limit</code>; <code>info</code> — <code>name</code> и <code>type</code>; <code>getMembers</code>/<code>getConstructors</code> — <code>typeName</code>; <code>getMember</code> — <code>typeName</code> и <code>memberName</code>.</p>

<h3>Навигация по BSL-коду (через bsl-lsp-bridge, 14 инструментов)</h3>
<table>
<tr><th>Инструмент</th><th>Описание</th></tr>
<tr><td><code>symbol_explore</code></td><td>Семантический поиск символов (процедур, функций, переменных) в BSL-коде проекта.</td></tr>
<tr><td><code>definition</code></td><td>Переход к определению символа - показывает файл и строку, где объявлена процедура/функция.</td></tr>
<tr><td><code>hover</code></td><td>Информация о символе под курсором: тип, параметры, документация.</td></tr>
<tr><td><code>call_hierarchy</code></td><td>Дерево вызовов: кто вызывает данную процедуру (incoming) и что она вызывает (outgoing).</td></tr>
<tr><td><code>call_graph</code></td><td>Построение графа вызовов с определением точек входа. Показывает цепочки вызовов между модулями.</td></tr>
<tr><td><code>document_diagnostics</code></td><td>Ошибки и предупреждения в конкретном BSL-файле (синтаксические ошибки, стилевые замечания).</td></tr>
<tr><td><code>project_analysis</code></td><td>Общий анализ проекта: количество символов, связи между модулями, структура.</td></tr>
<tr><td><code>lsp_status</code></td><td>Статус языкового сервера BSL: версия, состояние индексации, количество проиндексированных файлов.</td></tr>
<tr><td><code>prepare_rename</code></td><td>Проверка возможности переименования символа перед выполнением.</td></tr>
<tr><td><code>rename</code></td><td>Переименование символа (процедуры, функции, переменной) во всех местах использования.</td></tr>
<tr><td><code>selection_range</code></td><td>Определение диапазона выделения для символа (smart selection).</td></tr>
<tr><td><code>get_range_content</code></td><td>Получение содержимого указанного диапазона строк в файле.</td></tr>
<tr><td><code>code_actions</code></td><td>Доступные действия рефакторинга для выбранного фрагмента кода.</td></tr>
<tr><td><code>did_change_watched_files</code></td><td>Уведомление LSP об изменении файлов (для переиндексации).</td></tr>
</table>

<h3>Шлюзовые инструменты (19 инструментов + <code>its_search</code> при настроенном <code>NAPARNIK_API_KEY</code>)</h3>
<table>
<tr><th>Инструмент</th><th>Описание</th></tr>
<tr><td><code>get_server_status</code></td><td>Статус всех MCP-бэкендов: доступность, количество инструментов.</td></tr>
<tr><td><code>export_bsl_sources</code></td><td>Выгрузка исходников конфигурации 1С через 1cv8 DESIGNER /DumpConfigToFiles. По умолчанию запускается в фоне; для синхронного ожидания передайте <code>wait=true</code>.</td></tr>
<tr><td><code>get_export_status</code></td><td>Статус фоновой выгрузки BSL и последующей индексации для указанной строки подключения.</td></tr>
<tr><td><code>connect_database</code></td><td>Подключение новой базы 1С: регистрация, создание Docker-контейнеров.</td></tr>
<tr><td><code>disconnect_database</code></td><td>Мягкое отключение базы: остановка контейнеров выполнения и служебных подключений без удаления из реестра.</td></tr>
<tr><td><code>switch_database</code></td><td>Переключение активной базы для текущей MCP-сессии с маршрутизацией в пределах этой сессии.</td></tr>
<tr><td><code>list_databases</code></td><td>Список всех зарегистрированных баз и их статусы подключения.</td></tr>
<tr><td><code>validate_query</code></td><td>Проверка синтаксиса запроса 1С без выполнения: статические проверки (скобки, ключевые слова) + серверная валидация через <code>ПЕРВЫЕ 0</code>.</td></tr>
<tr><td><code>reindex_bsl</code></td><td>Принудительная переиндексация BSL Language Server. Используйте после ручного изменения файлов, git pull или внешней выгрузки.</td></tr>
<tr><td><code>write_bsl</code></td><td>Запись BSL-модуля в проект с автоматической переиндексацией LSP.</td></tr>
<tr><td><code>bsl_index</code></td><td>Построение полнотекстового поискового индекса по BSL-исходникам: все процедуры, функции, комментарии.</td></tr>
<tr><td><code>bsl_search_tool</code></td><td>Поиск процедур и функций в индексе BSL. Поддерживает фильтрацию по экспортным символам.</td></tr>
<tr><td><code>enable_anonymization</code></td><td>Включение маскировки персональных данных в ответах инструментов.</td></tr>
<tr><td><code>disable_anonymization</code></td><td>Выключение маскировки персональных данных.</td></tr>
<tr><td><code>its_search</code></td><td>Поиск по документации ИТС (1С:Информационно-технологическое сопровождение) через API 1С:Напарника. Требует настройки <code>NAPARNIK_API_KEY</code>.</td></tr>
<tr><td><code>invalidate_metadata_cache</code></td><td>Очистка кеша метаданных. Аналог кнопки «Очистить кеш» в дашборде.</td></tr>
<tr><td><code>query_stats</code></td><td>Статистика производительности запросов: количество, среднее/максимальное время, процент ошибок.</td></tr>
<tr><td><code>graph_stats</code></td><td>Статистика встроенного графа зависимостей BSL: количество узлов, рёбер, распределение по типам объектов. Требует профиль <code>bsl-graph</code>.</td></tr>
<tr><td><code>graph_search</code></td><td>Поиск объектов конфигурации во встроенном графе зависимостей. Требует профиль <code>bsl-graph</code>.</td></tr>
<tr><td><code>graph_related</code></td><td>Поиск связанных объектов во встроенном графе зависимостей: что использует объект и что его использует. Нужен для анализа влияния изменений. Требует профиль <code>bsl-graph</code>.</td></tr>
</table>

<h3>Веб-интерфейс графа</h3>
<p>Когда профиль <code>bsl-graph</code> запущен, по адресу <a href="http://localhost:8888/" target="_blank" rel="noopener"><code>http://localhost:8888/</code></a> открывается встроенный интерактивный просмотрщик графа (cytoscape.js, bundled, работает оффлайн). Визуально он оформлен в той же slate-палитре, что и этот дашборд; при клике на ссылку из карточки <i>Опциональные сервисы</i> автоматически подхватывается текущая локаль интерфейса (RU/EN).</p>
<p><b>Тулбар:</b> поле поиска с токенизацией по пробелам — запрос «размеры пособий» находит <code>РазмерыГосударственныхПособий</code>; кнопки ◀ ▶ навигируют историю поиска как в браузере; кнопка <b>Список</b> открывает плавающую панель с результатами последнего поиска (клик по строке — камера летит к узлу); <b>Очистить</b> сбрасывает канвас; <b>Пересобрать</b> / <b>Rebuild</b> — синхронный <code>POST /api/graph/rebuild</code>. Селектор <b>RU/EN</b> переключает язык (полный reload страницы).</p>
<p><b>Левая панель</b> содержит <i>Базы</i> (радиокнопки — одна база одновременно, по умолчанию первая; при единственной базе радио неактивно) и <i>Типы</i> (количество объектов по выбранной БД; клик — показать 30 узлов этого типа). <b>Центральный канвас</b> — cytoscape.js с layout'ом <code>cose</code>, плавная анимация 600–700 мс ease-out, smooth-zoom колёсиком (<code>wheelSensitivity: 0.2</code>). Заливка узла = тип объекта, цвет обводки = база. Длинные CamelCase-имена автоматически разбиваются по границам регистра. Одинарный клик — детали узла справа + подсветка его соседей (остальной граф приглушается). Двойной клик — drill-down через <code>/api/graph/related/&lt;id&gt;</code>. Лимит 200 узлов на канвасе.</p>
<p><b>Правая панель</b>: узел (type, db, id, path) + кнопка <b>Развернуть соседей</b>; легенда «цвет обводки — база данных». <b>Плавающая панель «Список»</b> открывается кнопкой в тулбаре и показывает все объекты последнего поиска с цветными точками (заливка = тип, обводка = база).</p>
<p><b>Интеграция с шлюзом:</b> сразу после успешной выгрузки BSL шлюз вызывает <code>POST /api/graph/rebuild</code>, и граф актуализируется без ожидания автоцикла. Файлы <code>.bsl</code> в графе получают читаемые имена вида <code>ОбъектИмя/Форма/Module.bsl</code> — поиск и список становятся различимыми. В карточке <i>Опциональные сервисы</i> имя <code>bsl-graph</code> становится кликабельной ссылкой, когда контейнер активен, и обычным текстом — когда выключен. При смене <code>BSL_WORKSPACE</code> / <code>BSL_HOST_WORKSPACE</code> через дашборд шлюз автоматически пересоздаёт <code>bsl-graph</code>, чтобы Docker обновил bind mount. Если вы редактируете <code>.env</code> вручную или обходите дашборд, используйте <code>docker compose --profile bsl-graph up -d --build --force-recreate bsl-graph</code> как резервный вариант.</p>

<h3>MCP-ресурс</h3>
<p>Шлюз также предоставляет один MCP-ресурс: <code>syntax_1c.txt</code> - справочник синтаксиса встроенного языка 1С (BSL): типы, операторы, управляющие конструкции, процедуры, исключения, директивы препроцессора. AI использует его как контекст при написании BSL-кода.</p>

<!-- ================================================================== -->
<h2 id="api-ru">7. API-эндпоинты</h2>
<p>HTTP-эндпоинты шлюза. Основной вход для AI-ассистентов - <code>/mcp</code>. Остальные эндпоинты используются дашбордом, обработкой EPF или внешними системами.</p>
<table>
<tr><th>Путь</th><th>Метод</th><th>Описание</th></tr>
<tr><td><code>/mcp</code></td><td>POST/GET</td><td>MCP Streamable HTTP - основная точка входа для AI-ассистентов. Поддерживает сессии с состоянием и таймаутом простоя 8 часов.</td></tr>
<tr><td><code>/health</code></td><td>GET</td><td>JSON-статус всех бэкендов. Возвращает <code>{"status":"ok"}</code> если все бэкенды доступны, <code>{"status":"degraded"}</code> если хотя бы один недоступен.</td></tr>
<tr><td><code>/dashboard</code></td><td>GET</td><td>Веб-интерфейс дашборда. Параметр <code>?lang=ru|en</code> для переключения языка.</td></tr>
<tr><td><code>/dashboard/docs</code></td><td>GET</td><td>Эта страница документации. Параметр <code>?lang=ru|en</code>.</td></tr>
<tr><td><code>/dashboard/diagnostics</code></td><td>GET</td><td>Полный диагностический отчёт в формате JSON (открывается в новой вкладке).</td></tr>
<tr><td><code>/api/export-bsl</code></td><td>POST</td><td>REST для выгрузки BSL-исходников. Вызывается обработкой MCPToolkit.epf при нажатии «Выгрузить BSL». Тело: <code>{"connection":"...","output_dir":"..."}</code>.</td></tr>
<tr><td><code>/api/reports/{action}</code></td><td>POST</td><td>REST-обёртка над инструментами отчётов: <code>analyze</code>, <code>find</code>, <code>list</code>, <code>describe</code>, <code>runner-policy</code>, <code>set-runner-policy</code>, <code>ui-strategy</code>, <code>set-ui-strategy</code>, <code>run</code>, <code>validate-all</code>, <code>validate-contracts</code>, <code>result</code>, <code>campaign</code>, <code>explain</code>. Контракт повторяет соответствующие MCP-инструменты отчётов.</td></tr>
<tr><td><code>/api/register</code></td><td>POST</td><td>Регистрация обработки EPF в шлюзе. Вызывается MCPToolkit.epf при нажатии «Подключиться». Тело: <code>{"name":"...","connection":"...","channel":"..."}</code>, где <code>channel</code> опционален, но рекомендуется для точной привязки к текущему экземпляру формы. Если база не подключена - автоматически подключает.</td></tr>
<tr><td><code>/api/unregister</code></td><td>POST</td><td>Отмена регистрации EPF. Вызывается при нажатии «Отключиться» в обработке. Тело: <code>{"name":"..."}</code>.</td></tr>
<tr><td><code>/api/action/connect-db</code></td><td>POST</td><td>Подключение базы данных из дашборда. Тело: <code>{"name":"...","connection":"...","project_path":"..."}</code>.</td></tr>
<tr><td><code>/api/action/disconnect</code></td><td>POST</td><td>Отключение базы. Параметр: <code>?name=имя_базы</code>.</td></tr>
<tr><td><code>/api/action/switch</code></td><td>POST</td><td>Установка базы по умолчанию. Параметр: <code>?name=имя_базы</code>.</td></tr>
<tr><td><code>/api/action/edit-db</code></td><td>POST</td><td>Редактирование параметров базы. Тело: <code>{"name":"...","connection":"...","project_path":"..."}</code>.</td></tr>
<tr><td><code>/api/action/clear-cache</code></td><td>POST</td><td>Очистка кеша метаданных.</td></tr>
<tr><td><code>/api/action/toggle-anon</code></td><td>POST</td><td>Переключение анонимизации (вкл/выкл).</td></tr>
<tr><td><code>/api/action/get-env</code></td><td>POST</td><td>Чтение содержимого файла <code>.env</code> для отображения в редакторе дашборда.</td></tr>
<tr><td><code>/api/action/save-env</code></td><td>POST</td><td>Сохранение файла <code>.env</code> с автоперезапуском шлюза. Дашборд отправляет полный текст файла в режиме замены: <code>{"content":"...","mode":"replace"}</code>. Неполные patch-обновления отклоняются.</td></tr>
</table>

<!-- ================================================================== -->
<h2 id="env-ru">8. Переменные окружения (.env)</h2>
<p>Все параметры настраиваются через файл <code>.env</code> в корне проекта. Файл монтируется в контейнер шлюза. Редактировать можно из дашборда (вкладка «Настройки» -> «Редактировать») или напрямую на хосте.</p>
<table>
<tr><th>Переменная</th><th>По умолч.</th><th>Описание</th></tr>
<tr><td><code>PORT</code></td><td>8080</td><td>Порт, на котором слушает MCP-шлюз. AI-ассистент подключается к <code>http://localhost:{PORT}/mcp</code>.</td></tr>
<tr><td><code>LOG_LEVEL</code></td><td>INFO</td><td>Уровень логирования: <code>DEBUG</code> (максимум деталей), <code>INFO</code>, <code>WARNING</code>, <code>ERROR</code> (только ошибки). Для отладки проблем установите <code>DEBUG</code>.</td></tr>
<tr><td><code>LOG_JSON</code></td><td>false</td><td>Включает structured JSON logging. Полезно для ELK/Loki/Cloud logging; по умолчанию используется человекочитаемый формат.</td></tr>
<tr><td><code>ONEC_TOOLKIT_URL</code></td><td>http://onec-toolkit:6003/mcp</td><td>URL статического бэкенда onec-toolkit внутри Docker-сети. Изменяйте только если используете нестандартную конфигурацию контейнеров.</td></tr>
<tr><td><code>PLATFORM_CONTEXT_URL</code></td><td>http://platform-context:8080/sse</td><td>URL бэкенда документации платформы внутри Docker-сети.</td></tr>
<tr><td><code>ENABLED_BACKENDS</code></td><td>onec-toolkit,platform-context,bsl-lsp-bridge</td><td>Список включённых бэкендов через запятую. Чтобы добавить YaXUnit, укажите: <code>onec-toolkit,platform-context,bsl-lsp-bridge,test-runner</code>.</td></tr>
<tr><td><code>EXPORT_HOST_URL</code></td><td>http://localhost:8082</td><td>URL сервиса выгрузки BSL, запущенного на хосте (<code>tools/export-host-service.py</code>). На Windows: <code>http://host.docker.internal:8082</code>.</td></tr>
<tr><td><code>DOCKER_CONTROL_URL</code></td><td>http://localhost:8091</td><td>URL внутреннего sidecar-сервиса <code>docker-control</code>, через который gateway управляет контейнерами, читает Docker diagnostics и проксирует LSP stdio. На Windows/macOS: <code>http://docker-control:8091</code> внутри bridge-сети; host port <code>8091</code> не публикуется.</td></tr>
<tr><td><code>DOCKER_CONTROL_TOKEN</code></td><td>авто</td><td>Общий bearer token для внутреннего вызова <code>gateway -> docker-control</code>. <code>GET /health</code> остаётся открытым, а все <code>/api/*</code> запросы к sidecar требуют <code>Authorization: Bearer ...</code>. В редакторе дашборда значение маскируется.</td></tr>
<tr><td><code>ANONYMIZER_SALT</code></td><td>авто</td><td>Секретный salt для стабильного маскирования ПД между перезапусками gateway. Генерируется <code>setup.sh</code> и маскируется в редакторе дашборда.</td></tr>
<tr><td><code>ALLOW_CONTAINER_DESIGNER_EXPORT</code></td><td>false</td><td>Защитный флаг. По умолчанию выгрузка через <code>1cv8</code> внутри контейнера запрещена (чтобы не сжечь software-лицензию). Включайте <code>true</code> только для HASP/CI.</td></tr>
<tr><td><code>PLATFORM_PATH</code></td><td>/opt/1cv8/x86_64/8.3.27.2074</td><td>Путь к платформе 1С. Используется для BSL-выгрузки. Шлюз автоматически сканирует /opt/1cv8/x86_64/ и находит нужную версию 1cv8.</td></tr>
<tr><td><code>BSL_WORKSPACE</code></td><td>/workspace</td><td>Рабочий каталог BSL-исходников внутри LSP-контейнера. Сюда монтируются исходники конфигурации.</td></tr>
<tr><td><code>BSL_HOST_WORKSPACE</code></td><td>—</td><td>Путь к BSL-исходникам на хосте. Используется для преобразования путей между контейнером и хостом (чтобы AI видел реальные пути файлов).</td></tr>
<tr><td><code>BSL_LSP_COMMAND</code></td><td>—</td><td>Команда для прямого запуска BSL Language Server в all-in-one режиме. В обычной gateway-архитектуре LSP-контейнеры создаются автоматически для каждой подключённой базы.</td></tr>
<tr><td><code>NAPARNIK_API_KEY</code></td><td>—</td><td>Ключ API для 1С:Напарника (поиск по ИТС). Получите на <a href="https://code.1c.ai">code.1c.ai</a> -> Профиль -> API-токен. Требует подписку ИТС.</td></tr>
<tr><td><code>TOOLKIT_ALLOW_DANGEROUS_WITH_APPROVAL</code></td><td>true</td><td>Разрешает toolkit-proxy передавать dangerous write/exec команды в 1С. Итоговое решение всё равно принимает <code>MCPToolkit.epf</code> по галочкам авторазрешения и диалогу подтверждения. Установите <code>false</code>, только если хотите жёстко блокировать такие команды ещё на уровне proxy.</td></tr>
<tr><td><code>GATEWAY_RATE_LIMIT_ENABLED</code></td><td>true</td><td>Включает rate limiting для REST API и дашборда. MCP endpoint <code>/mcp</code> и <code>/health</code> не ограничиваются.</td></tr>
<tr><td><code>GATEWAY_RATE_LIMIT_READ_RPM</code></td><td>120</td><td>Лимит чтения в минуту для маршрутов <code>/api/*</code> и <code>/dashboard*</code>.</td></tr>
<tr><td><code>GATEWAY_RATE_LIMIT_MUTATING_RPM</code></td><td>30</td><td>Лимит mutating-запросов в минуту для dashboard/API write operations.</td></tr>
<tr><td><code>METADATA_CACHE_TTL</code></td><td>600</td><td>Время жизни кеша метаданных в секундах. 600 = 10 минут. Значение <code>0</code> полностью отключает кеширование.</td></tr>
<tr><td><code>TEST_RUNNER_URL</code></td><td>http://localhost:8000/sse</td><td>URL бэкенда mcp-onec-test-runner для запуска тестов YaXUnit. Опционально.</td></tr>
<tr><td><code>BSL_GRAPH_URL</code></td><td>http://localhost:8888</td><td>URL встроенного сервиса графа зависимостей. Опционально, включается профилем <code>bsl-graph</code>. Когда сервис запущен, на этом адресе доступен встроенный веб-интерфейс визуализации графа (cytoscape.js, работает оффлайн); в карточке «Опциональные сервисы» имя <code>bsl-graph</code> становится кликабельной ссылкой.</td></tr>
<tr><td><code>PLATFORM_PATH</code></td><td>/opt/1cv8/x86_64/8.3.27.2074</td><td>Полный путь к каталогу конкретной версии платформы 1С. Используется бэкендом platform-context для чтения документации.</td></tr>
<tr><td><code>HOST_PLATFORM_PATH</code></td><td>/opt/1cv8</td><td>Корневой путь к платформе 1С на хосте. Монтируется в контейнеры для доступа к ibcmd и другим утилитам.</td></tr>
<tr><td><code>ONEC_TIMEOUT</code></td><td>180</td><td>Таймаут выполнения команд в 1С (секунды). Если запрос или код не завершится за это время, будет возвращена ошибка таймаута.</td></tr>
</table>

<!-- ================================================================== -->
<h2 id="diagnostics-ru">9. Диагностика</h2>
<p>Ссылка «Диагностика» расположена в нижней части дашборда (footer). Она открывает полный диагностический отчёт в новой вкладке браузера в формате JSON.</p>

<h3>Что содержит отчёт</h3>
<table>
<tr><th>Раздел</th><th>Содержимое</th></tr>
<tr><td><b>gateway</b></td><td>Версия шлюза, порт, количество активных MCP-сессий, настройка idle timeout.</td></tr>
<tr><td><b>backends</b></td><td>Статус каждого бэкенда: доступность, количество инструментов, ошибки. Аналог эндпоинта <code>/health</code>.</td></tr>
<tr><td><b>databases</b></td><td>Список всех подключённых баз: имя, строка подключения, путь к проекту, статус EPF, порт toolkit-бэкенда, имя LSP-контейнера.</td></tr>
<tr><td><b>profiling</b></td><td>Статистика execute_query: количество, среднее/макс/мин время, медленные запросы, процент ошибок.</td></tr>
<tr><td><b>cache</b></td><td>Состояние кеша метаданных: количество записей, hit rate, TTL, размер в памяти.</td></tr>
<tr><td><b>anonymization</b></td><td>Включена ли анонимизация, количество замен в текущей сессии.</td></tr>
<tr><td><b>docker</b></td><td>Информация о Docker-демоне: версия, ОС, количество CPU, объём RAM, размер образов и томов.</td></tr>
<tr><td><b>containers</b></td><td>Список контейнеров проекта: имя, образ, статус, время запуска.</td></tr>
<tr><td><b>config</b></td><td>Все переменные окружения (кроме секретных, которые маскируются как <code>***</code>).</td></tr>
<tr><td><b>container_logs</b></td><td>Последние 10 строк логов каждого контейнера проекта. Помогает быстро увидеть ошибки без использования <code>docker logs</code>.</td></tr>
</table>

<h3>Когда использовать</h3>
<ul>
<li>При поиске причины проблемы - отчёт содержит всю необходимую информацию в одном месте</li>
<li>При обращении за помощью - скопируйте JSON-отчёт и приложите к описанию проблемы</li>
<li>Для мониторинга - периодическая проверка состояния всех компонентов</li>
</ul>
<p>Диагностика также доступна через API: <code>POST /api/action/diagnostics</code>.</p>

<!-- ================================================================== -->
<h2 id="troubleshooting-ru">10. Устранение неполадок</h2>

<h3>Бэкенд показывает красный статус</h3>
<p><b>Симптом:</b> На вкладке «Статус» один или несколько бэкендов отображаются с красным индикатором.</p>
<p><b>Причины и решения:</b></p>
<ol>
<li>Контейнер не запущен. Проверьте: <code>docker compose ps</code>. Если контейнер остановлен - запустите: <code>docker compose up -d</code>.</li>
<li>Контейнер упал с ошибкой. Посмотрите логи: <code>docker logs onec-mcp-gw -f</code> (для шлюза), <code>docker logs onec-mcp-toolkit -f</code> (для toolkit). Частая причина - нехватка памяти.</li>
<li>Сетевая проблема между контейнерами. Убедитесь, что контейнеры находятся в одной Docker-сети: <code>docker network ls</code>.</li>
</ol>

<h3>Обработка EPF не подключается</h3>
<p><b>Симптом:</b> В обработке MCPToolkit.epf при нажатии «Подключиться» появляется ошибка или статус остаётся «Не подключено».</p>
<p><b>Причины и решения:</b></p>
<ol>
<li>Шлюз не запущен. Проверьте: <code>curl http://localhost:8080/health</code>. Должен вернуть JSON.</li>
<li>Неверный адрес шлюза в обработке. Убедитесь, что поле «Адрес шлюза» содержит правильный URL (по умолчанию <code>http://localhost:8080</code>).</li>
<li>Брандмауэр блокирует порт. На Windows проверьте, разрешён ли порт 8080 в настройках брандмауэра.</li>
<li>1С запущена в режиме тонкого клиента с ограничениями HTTP. Попробуйте использовать толстый клиент.</li>
</ol>

<h3>BSL-навигация не работает</h3>
<p><b>Симптом:</b> Инструменты <code>definition</code>, <code>symbol_explore</code>, <code>call_hierarchy</code> возвращают пустые результаты или ошибки.</p>
<p><b>Причины и решения:</b></p>
<ol>
<li>Исходники не выгружены. Нажмите «Выгрузить BSL» в обработке MCPToolkit.epf. Дождитесь завершения (3-5 минут для крупных конфигураций).</li>
<li>LSP ещё индексирует файлы. Проверьте статус: вызовите инструмент <code>lsp_status</code>. Индексация крупных проектов (ERP - 18 000 модулей) занимает несколько минут.</li>
<li>LSP-контейнер не запущен. Проверьте: <code>docker ps | grep mcp-lsp</code>.</li>
<li>Путь к проекту указан неверно. Убедитесь, что каталог BSL-исходников на хосте содержит файлы <code>.bsl</code> и корректно монтируется в контейнер.</li>
</ol>

<h3>Ошибка при редактировании .env в дашборде</h3>
<p><b>Симптом:</b> При нажатии «Редактировать» в конфигурации шлюза появляется сообщение «.env file not found».</p>
<p><b>Причины и решения:</b></p>
<ol>
<li>Файл .env не существует. Создайте: <code>cp .env.example .env</code> в корне проекта.</li>
<li>Файл не монтируется в контейнер. Проверьте, что в <code>docker-compose.yml</code> gateway получает <code>./.env:/data/.env:ro</code>, а <code>docker-control</code> — <code>./.env:/data/.env:rw</code>.</li>
<li>Нет прав на запись. Проверьте права: <code>ls -la .env</code>. Файл должен быть доступен для записи.</li>
</ol>

<h3>AI получает устаревшие метаданные</h3>
<p><b>Симптом:</b> AI утверждает, что у объекта есть реквизит, которого нет (или наоборот).</p>
<p><b>Причины и решения:</b></p>
<ol>
<li>Кеш метаданных содержит устаревшие данные. Очистите кеш: кнопка «Очистить кеш» на вкладке «Настройки» или инструмент <code>invalidate_metadata_cache</code>.</li>
<li>Уменьшите TTL кеша в настройках: <code>METADATA_CACHE_TTL=60</code> (1 минута вместо 10).</li>
</ol>

<h3>Запросы execute_query выполняются медленно</h3>
<p><b>Симптом:</b> Запросы занимают более 5 секунд, карточка «Профилирование» показывает большие числа.</p>
<p><b>Причины и решения:</b></p>
<ol>
<li>Неоптимальные запросы от AI. Проверьте поле <code>_profiling</code> в ответе - оно содержит подсказки (SELECT *, отсутствие WHERE, много JOIN).</li>
<li>Нагрузка на сервер 1С. Запросы выполняются в контексте информационной базы и конкурируют с другими пользователями.</li>
<li>Сетевые задержки. Если сервер 1С находится на удалённом хосте - проверьте ping и пропускную способность.</li>
<li>Увеличьте таймаут: <code>ONEC_TIMEOUT=300</code> (5 минут вместо 3).</li>
</ol>

<h3>Обработка показывает «Нет ответа от шлюза» (таймаут)</h3>
<p><b>Симптом:</b> Обработка MCPToolkit.epf периодически выводит в журнал ошибку таймаута.</p>
<p><b>Причины и решения:</b></p>
<ol>
<li>Шлюз перегружен или перезапускается. Проверьте логи: <code>docker logs onec-mcp-gw --tail 50</code>.</li>
<li>Long-polling соединение разорвалось. Нажмите «Отключиться» и затем «Подключиться» повторно.</li>
<li>При самом первом подключении после холодного запуска подождите дольше обычного: регистрация EPF допускает до 240 секунд, пока шлюз поднимает контур выполнения для базы.</li>
<li>Проблемы с Docker-сетью. Перезапустите контейнеры: <code>docker compose restart</code>.</li>
</ol>

<h3>Docker-контейнеры не создаются при подключении базы</h3>
<p><b>Симптом:</b> При подключении базы появляется ошибка «Failed to create container».</p>
<p><b>Причины и решения:</b></p>
<ol>
<li>Недостаточно ресурсов Docker. Проверьте свободное место: <code>docker system df</code>. Очистите неиспользуемые образы: <code>docker system prune</code>.</li>
<li>Недоступен <code>docker-control</code>. На Linux проверьте <code>http://127.0.0.1:8091/health</code>. На Windows/macOS используйте <code>./verify-install.sh</code> / <code>verify-install.ps1</code> — sidecar там проверяется изнутри <code>onec-mcp-gw</code>. Убедитесь, что <code>DOCKER_CONTROL_TOKEN</code> существует в <code>.env</code>. Прямой <code>docker.sock</code> в gateway больше не используется.</li>
<li>Конфликт имён. Контейнер с таким именем уже существует. Удалите вручную: <code>docker rm -f onec-toolkit-{имя_базы}</code>.</li>
</ol>

</body></html>""",

    "en": r"""<!DOCTYPE html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Documentation - onec-mcp-universal</title>
<style>""" + _DOC_STYLE + """</style></head><body>
<a class="back" href="/dashboard?lang=en">&larr; Back to dashboard</a>
<h1>onec-mcp-universal documentation</h1>
<p>Version: """ + VERSION + """ | <a href="https://github.com/AlekseiSeleznev/onec-mcp-universal">GitHub</a> | License: MIT</p>

<h2>Contents</h2>
<ol>
<li><a href="#overview-en">Overview</a> - what it is and how it is wired</li>
<li><a href="#status-en">Status tab</a> - databases, backends, reports, Docker, and current metrics</li>
<li><a href="#params-en">Settings tab</a> - databases, .env, and quick actions</li>
<li><a href="#epf-en">MCPToolkit.epf data processor</a> - connection, buttons, security, and event log</li>
<li><a href="#remote-en">Remote deployment</a> - running on a 1C server and the limits of that mode</li>
<li><a href="#tools-en">MCP tools</a> - tool list</li>
<li><a href="#api-en">API endpoints</a> - gateway HTTP API</li>
<li><a href="#env-en">Environment variables</a> - .env settings</li>
<li><a href="#diagnostics-en">Diagnostics</a> - what goes into the JSON report</li>
<li><a href="#troubleshooting-en">Troubleshooting</a> - common failures and what to check</li>
</ol>

<!-- ================================================================== -->
<h2 id="overview-en">1. Overview</h2>

<h3>What it is</h3>
<p><b>onec-mcp-universal</b> is an MCP gateway for working with 1C:Enterprise databases from AI assistants and other MCP clients. MCP (Model Context Protocol) gives the model a way to call external tools: run queries, read metadata, navigate BSL code, and collect diagnostics.</p>

<h3>Why a gateway is needed</h3>
<p>Without the gateway, 1C data, code navigation, and platform documentation would be separate MCP connections. Here they share one entry point: <code>http://localhost:8080/mcp</code>. The AI assistant connects once and sees the tools enabled for the current installation. The exact count depends on settings, for example whether <code>its_search</code> is enabled through <code>NAPARNIK_API_KEY</code>.</p>

<h3>How it works</h3>
<p>The interaction chain looks like this:</p>
<pre><code>AI assistant / MCP client
    |
    | MCP Streamable HTTP
    v
MCP gateway (onec-mcp-universal, port 8080)
    |
    +---> onec-toolkit      - 1C data (queries, code, metadata)
    +---> platform-context  - platform API documentation
    +---> bsl-lsp-bridge    - BSL code navigation
    +---> test-runner       - YaXUnit test execution (optional)
    |
    v
MCPToolkit.epf (data processor inside 1C client)
    |
    v
1C:Enterprise database</code></pre>

<p>When the AI assistant calls a tool, for example <code>execute_query</code>, the gateway chooses the backend and forwards the request. <code>onec-toolkit</code> sends the command to <code>MCPToolkit.epf</code>, which is running in the 1C client. The data processor performs the action in the database and returns the result through the gateway.</p>

<h3>Per-session routing</h3>
<p>Each AI assistant session has its own active database. Two Codex windows can work with different databases at the same time, for example ERP in one window and HRM in another, without mixing requests. All registered databases can stay connected. Use <code>switch_database</code> to switch a session. Session idle timeout: 8 hours.</p>

<div class="note"><p><b>Single connection point:</b> Regardless of how many databases and backends are connected, the AI assistant always uses a single address: <code>http://localhost:8080/mcp</code>.</p></div>

<!-- ================================================================== -->
<h2 id="status-en">2. Status tab</h2>
<p>The Status tab is the main dashboard screen. It shows the current state of databases, backends, reports, cache, anonymization, and Docker containers. This tab is read-only; actions live on the Settings tab or in MCP/API calls.</p>

<h3>Databases card</h3>
<p>A table of registered 1C databases. Each row shows:</p>
<table>
<tr><th>Column</th><th>Description</th></tr>
<tr><td><b>Name</b></td><td>Unique database identifier (Latin letters, digits, hyphens, underscores). Used in Docker container names: <code>onec-toolkit-{name}</code> and <code>mcp-lsp-{name}</code>. Examples: <code>ERP</code>, <code>ZUP_TEST</code>, <code>buh-main</code>.</td></tr>
<tr><td><b>Connection</b></td><td>1C connection string in standard platform format. For server databases: <code>Srvr=server_name;Ref=db_name;</code>. For file databases: <code>File=/path/to/db</code>.</td></tr>
<tr><td><b>EPF</b></td><td>MCPToolkit.epf connection status. Displayed as a colored indicator:<br>
<span style="color:#22c55e">Connected</span> (green) - the data processor is running in the 1C client, long-polling connection is active, AI can execute queries to this database.<br>
<span style="color:#eab308">Not connected</span> (yellow) - Docker containers for the database are running, but the data processor is not yet connected. You need to open MCPToolkit.epf in the 1C client and click "Connect".</td></tr>
</table>
<p>The default database has a badge <span style="background:#164e63;color:#22d3ee;padding:1px 6px;border-radius:3px;font-size:.75rem">default</span> next to its name. This database is used for new AI sessions unless the session switches to another database via <code>switch_database</code>.</p>
<div class="note"><p><b>Auto-connect:</b> If the user clicks "Connect" in the MCPToolkit.epf data processor and the database is not yet registered in the gateway, it is automatically connected. Docker containers are created and the database appears in this table.</p></div>

<h3>Profiling card</h3>
<p>Statistics for <code>execute_query</code> calls. The gateway collects them from startup and resets them on restart.</p>
<table>
<tr><th>Metric</th><th>Description</th></tr>
<tr><td><b>Queries</b></td><td>Total number of queries executed since gateway startup. All <code>execute_query</code> calls to all connected databases are counted.</td></tr>
<tr><td><b>Avg</b></td><td>Average query execution time in milliseconds. Includes time to send the query to 1C and receive the response. Values above 1000 ms signal a need for query optimization.</td></tr>
<tr><td><b>Max</b></td><td>Maximum execution time for a single query in milliseconds. Helps identify outliers, individual heavy queries.</td></tr>
<tr><td><b>Slow (&gt;5s)</b></td><td>Number of queries that took more than 5 seconds. Such queries are considered slow and typically require optimization (adding indexes, reworking conditions, using temp tables).</td></tr>
</table>
<p>If no queries have been executed yet, the card shows "No queries yet".</p>
<p>Besides the dashboard, profiling statistics can be obtained via the <code>query_stats</code> MCP tool. Each <code>execute_query</code> response automatically includes a <code>_profiling</code> field with execution duration and optimization hints (e.g. "query uses SELECT *", "missing WHERE", "many JOINs").</p>

<h3>Anonymization card</h3>
<p>Status of personal data masking in responses from 1C:</p>
<ul>
<li><span style="color:#22c55e">Enabled</span> (green) - all responses from <code>execute_query</code>, <code>execute_code</code>, <code>get_object_by_link</code>, <code>get_event_log</code> tools pass through the anonymization filter.</li>
<li><span style="color:#eab308">Disabled</span> (yellow) - data is passed to AI without modification.</li>
</ul>
<p><b>What is masked:</b></p>
<ul>
<li><b>Full names</b> - patterns like "Last First Middle" are recognized and replaced with stable fake data (e.g. Ivanov Ivan Ivanovich always becomes Petrov Sergey Alexandrovich)</li>
<li><b>INN (tax ID)</b> - 10-digit (legal entities) and 12-digit (individuals) numbers are replaced with valid fake INNs</li>
<li><b>SNILS (social security)</b> - numbers in format XXX-XXX-XXX XX</li>
<li><b>Phone numbers</b> - Russian numbers (+7...)</li>
<li><b>Email addresses</b></li>
<li><b>Company names</b> - legal entities (LLC, JSC, etc.)</li>
</ul>
<p><b>Stable mapping:</b> the same original value is replaced with the same fake value within a session. Object relationships stay intact: if a client had documents before masking, the masked client still has those documents.</p>
<p><b>When to use:</b> enable anonymization for production databases with real personal data. On test databases with synthetic data, it can usually stay off.</p>

<h3>Metadata cache card</h3>
<p>Results of <code>get_metadata</code> calls (configuration structure information: attributes, tabular sections, types) are cached on the gateway side to avoid requesting the same metadata from 1C repeatedly.</p>
<table>
<tr><th>Metric</th><th>Description</th></tr>
<tr><td><b>Entries</b></td><td>Number of cached metadata objects. Each unique <code>get_metadata</code> call with a specific set of parameters creates one cache entry.</td></tr>
<tr><td><b>Hit Rate</b></td><td>Percentage of requests served from cache. Values of 80-100% mean the cache is working effectively. A low percentage may mean AI is requesting different objects each time, or the TTL is too short.</td></tr>
<tr><td><b>TTL</b></td><td>Cache entry lifetime in seconds. Default is 600 seconds (10 minutes). Configured via the <code>METADATA_CACHE_TTL</code> variable in the <code>.env</code> file. Value of 0 completely disables caching.</td></tr>
</table>
<p><b>When to clear the cache:</b> After changing the 1C configuration structure (adding/removing attributes, tabular sections, metadata objects). You can clear it using the button on the Parameters tab, via the <code>invalidate_metadata_cache</code> MCP tool, or wait for automatic invalidation by TTL.</p>

<h3>Backends card</h3>
<p>A backend is a separate MCP server that provides a set of tools in a specific category. The gateway aggregates tools from all backends and presents them to the AI assistant as a single set.</p>
<p>Each row shows:</p>
<ul>
<li><b>Colored indicator</b> - <span style="color:#22c55e">green</span> = backend is available and responding, <span style="color:#ef4444">red</span> = backend is unavailable (container not running or an error occurred)</li>
<li><b>Name</b> - backend identifier</li>
<li><b>N tools</b> - number of tools the backend provides</li>
<li><b>"active" badge</b> - displayed for backends bound to the current active database</li>
</ul>
<p><b>Static backends</b> (created at gateway startup):</p>
<table>
<tr><th>Backend</th><th>Tools</th><th>Purpose</th></tr>
<tr><td><code>onec-toolkit</code></td><td>8</td><td>DB queries, code execution, metadata, event log, access rights, object links. Works through the MCPToolkit.epf data processor.</td></tr>
<tr><td><code>platform-context</code></td><td>5</td><td>1C built-in language API documentation: type method search, constructor descriptions, object member listings.</td></tr>
<tr><td><code>bsl-lsp-bridge</code></td><td>14</td><td>BSL code navigation: symbol search, go-to-definition, call graphs, error diagnostics, renaming.</td></tr>
</table>
<p><b>Dynamic backends</b> (created when each database is connected):</p>
<ul>
<li><code>onec-toolkit-{db_name}</code> - dedicated data backend for a specific database</li>
<li><code>mcp-lsp-{db_name}</code> - dedicated BSL Language Server with the configuration index of a specific database</li>
</ul>
<p><b>Optional backends</b> (enabled via <code>ENABLED_BACKENDS</code>):</p>
<ul>
<li><code>test-runner</code> - YaXUnit test execution. Add <code>test-runner</code> to <code>ENABLED_BACKENDS</code> and start: <code>docker compose --profile test-runner up -d</code>.</li>
</ul>

<h3>1C reports card</h3>
<p>This card shows a local summary of the report runtime. It does not execute reports, run analysis, or call 1C while rendering the page. Data comes from the SQLite catalog <code>/data/report-catalog.sqlite</code> and stored result artifacts in <code>/data/report-results</code>.</p>
<table>
<tr><th>Metric</th><th>Description</th></tr>
<tr><td><b>Last analysis</b></td><td>When the report catalog for a database was last rebuilt.</td></tr>
<tr><td><b>Reports / Variants</b></td><td>How many cataloged reports and variants were found in exported BSL/XML sources.</td></tr>
<tr><td><b>Checked / Results</b></td><td>How many cataloged reports already have a latest stored run and how many of those latest runs still have a readable result artifact.</td></tr>
<tr><td><b>Done / Needs input / Unsupported / Errors</b></td><td>Status distribution by the latest stored run of each report. In the underlying API these values correspond to <code>done</code>, <code>needs_input</code>, <code>unsupported</code>, and <code>error</code>.</td></tr>
</table>
<div class="note"><p><b>Important:</b> report discovery and execution are not performed from the dashboard. They remain available through MCP/API tools such as <code>find_reports</code>, <code>describe_report</code>, <code>run_report</code>, and related methods. The <b>"Refresh stats"</b> button refreshes this card from local gateway state; it does not make live 1C calls.</p></div>

<h3>Docker containers card</h3>
<p>The upper part of the card shows Docker daemon information: Docker version, CPU count, RAM, total image and volume sizes.</p>
<p>When the dashboard first opens, this card is rendered in a <b>lightweight mode</b>: expensive Docker statistics are not queried automatically, so the page stays responsive. RAM and image-size values are initially shown as <code>n/a</code>. To fetch them explicitly, use the <b>"Refresh stats"</b> button next to the regular page refresh button.</p>
<p>Below is a table of project containers with five columns:</p>
<table>
<tr><th>Column</th><th>Description</th></tr>
<tr><td><b>Container</b></td><td>Docker container name with a colored status indicator</td></tr>
<tr><td><b>Image</b></td><td>Docker image from which the container was created</td></tr>
<tr><td><b>RAM now</b></td><td>Current container RAM usage. This is a live runtime metric and changes over time.</td></tr>
<tr><td><b>Image on disk</b></td><td>Docker image size on disk. This is not RAM usage; it is local Docker storage usage.</td></tr>
<tr><td><b>Container status</b></td><td><code>running</code> or <code>stopped</code></td></tr>
</table>
<div class="note"><p><b>Important:</b> the <code>running</code> status in this table refers to the Docker container itself. It does not mean that the EPF processor is already connected for the corresponding database. EPF status is shown separately in the database cards.</p></div>
<p><b>Container types:</b></p>
<ul>
<li><code>onec-mcp-gw</code> - MCP gateway (this server, port 8080). Receives MCP requests from AI and routes them to backends.</li>
<li><code>onec-mcp-toolkit</code> - static data backend (port 6003). Processes 1C requests via long-polling with EPF.</li>
<li><code>onec-mcp-platform</code> - 1C platform documentation backend (port 8081). Contains structured built-in language API documentation.</li>
<li><code>onec-toolkit-{db_name}</code> - dynamic data backend, created automatically when each new database is connected.</li>
<li><code>mcp-lsp-{db_name}</code> - BSL Language Server for each database. Indexes configuration sources and provides code navigation.</li>
</ul>

<!-- ================================================================== -->
<h2 id="params-en">3. Settings tab</h2>
<p>The Settings tab contains actions that change gateway state: database connections, configuration editing, cache reset, and anonymization.</p>

<h3>Database management</h3>
<p>The database management card shows registered databases and control buttons. A disconnected database name is shown <span style="text-decoration:line-through;color:#64748b;font-weight:bold">bold and struck through</span> in both Status and Settings.</p>
<table>
<tr><th>Button</th><th>Description</th></tr>
<tr><td><b>Default</b></td><td>Sets the database as the default for new AI sessions. Shown only for databases that are not already the default.</td></tr>
<tr><td><b>Edit</b></td><td>Opens a dialog to edit the connection string and project path. After saving, reconnect the database to apply changes.</td></tr>
<tr><td><b>Reindex BSL</b></td><td>Triggers BSL re-indexing in the database's LSP container. Use after exporting new sources or updating the configuration.</td></tr>
<tr><td><b style="color:#ef4444">Disconnect</b></td><td>Soft disconnect: stops Docker containers and removes active backends, but <b>keeps the registry entry</b>. The button changes to "Connect" afterwards. 1C data, physical BSL files, and graph/state are left intact.</td></tr>
<tr><td><b>Connect</b></td><td>Reconnects a disconnected database: restarts containers from saved configuration. The page refreshes automatically when containers are ready. MCPToolkit.epf in 1C reconnects on its own.</td></tr>
<tr><td><b style="color:#ef4444">Delete</b></td><td>Full deletion: stops containers, clears runtime/state/graph for that database, and removes the registry entry. Irreversible — you will need to re-fill the "Add Database" form to reconnect. 1C data and physical BSL files on disk are not affected.</td></tr>
</table>

<h3>Adding a database</h3>
<p>The lower part of the card contains the "Add Database" form:</p>
<table>
<tr><th>Field</th><th>Description</th><th>Example</th></tr>
<tr><td><b>DB name</b></td><td>Unique identifier. Latin letters, digits, hyphens, and underscores are allowed. Maximum 63 characters.</td><td><code>MY_INFOBASE</code></td></tr>
<tr><td><b>Connection string</b></td><td>Standard 1C platform connection string. Server: <code>Srvr=server_name;Ref=db_name;</code>. File: <code>File=/path/to/db</code>. With authentication: <code>Srvr=srv;Ref=db;Usr=admin;Pwd=password;</code></td><td><code>Srvr=localhost;Ref=MY_INFOBASE;</code></td></tr>
<tr><td><b>Project path</b></td><td>Absolute path on the host where BSL configuration sources will be exported. This directory is mounted into the LSP container for code navigation.</td><td><code>/home/user/projects/MY_INFOBASE</code></td></tr>
</table>
<p>After clicking the "Connect" button, the gateway will create two Docker containers for the database and register it. Then open MCPToolkit.epf in the 1C client and click "Connect".</p>

<h3>Three ways to connect a database</h3>
<p>A 1C database can be connected in three ways:</p>
<ol>
<li><b>From the MCPToolkit.epf data processor</b> (recommended) - open the data processor in the 1C:Enterprise client, click "Connect". The data processor will automatically determine the database name and server, register the database in the gateway, and create Docker containers. No manual configuration is required.</li>
<li><b>From the dashboard</b> - go to the Settings tab, fill in the "Add Database" form, and click "Connect". Then open MCPToolkit.epf in 1C and click "Connect" to establish the long-polling connection.</li>
<li><b>Via AI assistant</b> - type in the AI chat in natural language:
<pre><code>Connect database MY_INFOBASE, connection string Srvr=localhost;Ref=MY_INFOBASE;, folder /home/user/projects/MY_INFOBASE</code></pre>
The AI will call the <code>connect_database</code> tool with the specified parameters. Then open MCPToolkit.epf in 1C and click "Connect".</li>
</ol>

<h3>Gateway configuration</h3>
<p>The second card on the Settings tab shows current gateway environment variables from <code>.env</code>. The top row has action buttons for cache, anonymization, and editing.</p>
<p><b>Editing process:</b></p>
<ol>
<li>Click the <b>"Edit"</b> button. The table is replaced with a text editor showing the full contents of the <code>.env</code> file.</li>
<li>Make your changes. Format: <code>VARIABLE=value</code>, one variable per line. Lines starting with <code>#</code> are comments.</li>
<li>Click <b>"Save"</b>. The file is saved, and the gateway <b>automatically restarts</b> within a few seconds. All current MCP sessions will be disconnected (the AI assistant will reconnect automatically).</li>
<li>Or click <b>"Cancel"</b>. The editor closes without saving.</li>
</ol>
<div class="note"><p><b>Mounting:</b> the gateway reads <code>.env</code> as <code>./.env:/data/.env:ro</code>, while the <code>docker-control</code> sidecar gets <code>./.env:/data/.env:rw</code> only to write dashboard changes back to the host file. Works on Linux and Windows (via <code>docker-compose.windows.yml</code>).</p></div>

<h3>BSL export folder</h3>
<p>This card sets the host-side root folder for exported BSL sources. Each connected database gets its own subdirectory inside that root.</p>
<p>The <b>"Browse..."</b> button opens the standard operating-system directory chooser on the host itself. The chooser is provided by <code>export-host-service.py</code>, so <code>EXPORT_HOST_URL</code> must be configured and the export service must be reachable.</p>
<div class="note"><p><b>Important:</b> saving a new folder applies it without restarting the gateway. Running databases are switched to the new export root immediately, and the graph container is recreated so Docker picks up the new bind mount.</p></div>

<h3>1C report engine</h3>
<p>This card sets defaults used by report MCP/API tools. The dashboard does not run reports from here; it only saves settings later used by <code>run_report</code> and bulk validation.</p>
<table>
<tr><th>Parameter</th><th>Description</th></tr>
<tr><td><b>Auto-analyze after connect, export, and BSL reindex</b></td><td>If enabled, the gateway refreshes the report catalog automatically after database connection, successful source export, and explicit BSL reindex.</td></tr>
<tr><td><b>Use API runner</b></td><td>Enables the fast headless path through <code>toolkit</code>/<code>EPF</code>. This is the default report execution path.</td></tr>
<tr><td><b>Use UI runner</b></td><td>Enables execution through the 1C web client. The runner opens the report as a user, fills parameters, generates the report, and exports a temporary file for parsing.</td></tr>
<tr><td><b>Fallback to UI when API hits a technical limit</b></td><td>In <code>runner=auto</code> mode, allows the gateway to try the web UI when the API path cannot return a correct tabular result.</td></tr>
<tr><td><b>UI export format</b></td><td>Temporary artifact format for the UI runner: <code>XLSX</code> or <code>HTML</code>. The file is deleted after a successful parse.</td></tr>
<tr><td><b>Keep UI error files</b></td><td>Keeps temporary files only for failed UI runs, so they can be inspected later.</td></tr>
<tr><td><b>Rows per run / Run timeout</b></td><td>Default values for <code>run_report</code> when the caller does not pass its own <code>max_rows</code> and <code>timeout_seconds</code>.</td></tr>
<tr><td><b>Rows per validation / Validation timeout</b></td><td>Default values for <code>validate_all_reports</code>. They bound the size and duration of bulk validation runs.</td></tr>
</table>
<p>Saving this card does not restart the gateway: the new values are applied immediately and written to <code>.env</code> at the same time.</p>

<h3>Actions</h3>
<p>Quick action buttons are located above the configuration table:</p>
<table>
<tr><th>Action</th><th>Description</th><th>When to use</th></tr>
<tr><td><b>Clear Cache</b></td><td>Removes all cached <code>get_metadata</code> results. Subsequent metadata requests will go directly to 1C.</td><td>After changing the configuration structure: adding/removing attributes, tabular sections, metadata objects. Also useful when AI receives outdated structure information.</td></tr>
<tr><td><b>Toggle Anonymization</b></td><td>Toggles personal data masking in tool responses. Does not require gateway restart, takes effect immediately.</td><td>Enable when working with production databases containing real names, tax IDs, phone numbers. Disable when working with test databases or when masking interferes with data analysis.</td></tr>
</table>

<!-- ================================================================== -->
<h2 id="epf-en">4. MCPToolkit.epf Data Processor</h2>
<p>The external data processor <code>MCPToolkit.epf</code> is a key component that runs on the 1C side. It is launched in the 1C:Enterprise client and acts as a mediator between the MCP gateway and the database. Without it, operations with 1C data (queries, code execution, metadata reading) are impossible.</p>
<p>The data processor file is located in the project directory: <code>1c/MCPToolkit/build/MCPToolkit.epf</code>.</p>

<h3>Interface Fields</h3>
<table>
<tr><th>Field</th><th>Description</th><th>How to fill</th></tr>
<tr><td><b>Gateway address</b></td><td>MCP gateway URL. Used for database registration and sending results.</td><td>Default is <code>http://localhost:8080</code>. Change if the gateway runs on a different host or port. If <code>GATEWAY_API_TOKEN</code> is enabled, use <code>http://host:8080#token=&lt;secret&gt;</code>.</td></tr>
<tr><td><b>Database: name</b></td><td>Current database identifier. Determines which set of Docker containers the data processor will be bound to.</td><td>Auto-filled from the current database connection string. Read-only.</td></tr>
<tr><td><b>Database: server</b></td><td>1C server name or file database path.</td><td>Auto-detected. Read-only.</td></tr>
<tr><td><b>Database: user</b></td><td>Current 1C session user name.</td><td>Auto-detected. Read-only.</td></tr>
<tr><td><b>Database: password</b></td><td>Password for BSL source export via DESIGNER. Only needed for the "Export BSL" operation.</td><td>Enter manually. Not saved between sessions.</td></tr>
</table>

<h3>Buttons</h3>
<table>
<tr><th>Button</th><th>Action</th></tr>
<tr><td><b>Connect</b></td><td>Performs two actions:<br>1. <b>Registration</b> - sends database details (name, connection string, and the current form instance <code>channel</code>) to the gateway via <code>/api/register</code>. If the database is not yet connected, the gateway automatically creates Docker containers.<br>2. <b>Long-polling</b> - establishes a persistent connection with the toolkit backend. The data processor starts "listening" for incoming commands from AI and executing them in the context of the current database.</td></tr>
<tr><td><b>Disconnect</b></td><td>Stops the long-polling connection. The data processor stops accepting commands. Docker containers continue running. The database remains registered, but the EPF status changes to "Not connected".</td></tr>
<tr><td><b>Export BSL</b></td><td>Starts exporting 1C configuration sources to BSL files for code navigation. Uses <code>1cv8 DESIGNER /DumpConfigToFiles</code> with automatic platform version detection. On client/server version mismatch, the gateway finds and uses the matching binary. On large configurations (ERP, ~18,000 modules; HRM, ~12,000 modules), indexing takes 3-5 minutes. After completion, the BSL Language Server automatically re-indexes the files.</td></tr>
</table>

<h3>Auto-allow operations</h3>
<p>Three checkboxes control automatic approval of potentially dangerous operations when AI executes arbitrary code via the <code>execute_code</code> tool. On every fresh EPF open all three start <code>disabled</code> and apply only to the currently opened form instance:</p>
<table>
<tr><th>Checkbox</th><th>Description</th><th>Recommendation</th></tr>
<tr><td><b>Write object</b></td><td>If enabled, AI can write data (<code>Object.Write()</code>, <code>Document.Write(PostingMode.Posting)</code>) without manual operator confirmation. If disabled, each write attempt triggers a dialog in the 1C client asking "Allow write?".</td><td>On production databases: <b>disabled</b>. On test databases: at user's discretion.</td></tr>
<tr><td><b>Privileged mode</b></td><td>If enabled, AI can set privileged mode (<code>SetPrivilegedMode(True)</code>) without confirmation. Privileged mode disables access rights checking in 1C. Code executes with full permissions.</td><td>On production databases: <b>disabled</b>. Enable only when necessary and with understanding of the risks.</td></tr>
<tr><td><b>All dangerous operations without warning</b></td><td>Globally bypasses the dangerous-operation confirmation gate. When enabled, the form shows an extra confirmation dialog listing the affected actions: write, delete, file operations, COM/add-in calls, privileged mode, and exclusive mode.</td><td>On production databases: <b>disabled</b>. Enable only for controlled test scenarios.</td></tr>
</table>
<div class="warn"><p><b>Security:</b> On production databases with real data, it is recommended to keep all three checkboxes disabled. Each potentially dangerous operation will require manual operator confirmation in the 1C client. This prevents accidental deletion or modification of data.</p></div>

<h3>Status and Links</h3>
<p>The lower part of the form displays the current connection status (Connected / Disconnected) and links:</p>
<ul>
<li><b>Dashboard</b> - opens the gateway web interface (<code>/dashboard</code>) in the browser</li>
<li><b>GitHub</b> - link to the project repository</li>
</ul>

<h3>Event Log</h3>
<p>A unified log at the bottom of the form displays all events in chronological order:</p>
<ul>
<li>Successful connections and disconnections</li>
<li>Incoming commands from AI (tool name, parameters)</li>
<li>Command execution results</li>
<li>Errors (network, 1C exceptions, timeouts)</li>
<li>BSL export information</li>
<li>Remote connection warnings (if the gateway is on a different server)</li>
</ul>
<p>The log is useful for debugging. If the AI assistant receives an error, the log will show what exactly happened on the 1C side.</p>

<h3>Anonymization Tab</h3>
<p>An additional tab in the data processor allows configuring <b>precise anonymization</b> rules on the EPF side: regex patterns, replacement dictionaries, exclusion lists. This anonymization works <b>independently</b> from the gateway's server-side anonymization and can be used together with it or separately.</p>

<h3>Fallback Mode</h3>
<p>If the gateway is unavailable (network is down, containers are stopped, server is off), the MCPToolkit.epf data processor switches to <b>direct connection</b> to the onec-toolkit backend. In this mode the data processor works directly without the gateway.</p>
<p><b>How it works:</b></p>
<ol>
<li>When you click "Connect", the data processor checks gateway availability (5-second timeout).</li>
<li>If the gateway is reachable, the initial <code>/api/register</code> on a cold start may wait for up to 240 seconds while the gateway prepares toolkit/LSP containers and returns a backend bound to the current form <code>channel</code>.</li>
<li>If the gateway responds, the data processor works through the gateway as usual.</li>
<li>If the gateway does not respond, the data processor connects directly to the toolkit backend.</li>
<li>A warning appears in the event log: "Gateway unavailable, using direct toolkit connection".</li>
</ol>
<p><b>Fallback mode limitations:</b> Without the gateway, BSL navigation, platform documentation, anonymization, metadata caching, and profiling are unavailable. Only onec-toolkit tools work: DB queries, code execution, metadata, event log, access rights.</p>

<!-- ================================================================== -->
<h2 id="remote-en">5. Remote Deployment</h2>
<p>If the 1C server is on a remote machine and users connect to it via thin client or web client, the gateway can be deployed directly on that server. The AI assistant on the developer's workstation will connect to the gateway over the network.</p>

<h3>When this is needed</h3>
<p>Typical situation: the 1C server is in a server room or in the cloud. Users work via thin client. The AI assistant or MCP client runs on the developer's workstation. The 1C platform is not installed on the workstation - only the thin client. In this case, deploy the gateway on the 1C server, and the client will connect to it over the network through port 8080.</p>

<h3>Deployment on the 1C server</h3>
<ol>
<li>Install Docker and Docker Compose on the 1C server.</li>
<li>Clone the project: <code>git clone https://github.com/AlekseiSeleznev/onec-mcp-universal.git</code></li>
<li>Create the <code>.env</code> file: <code>cp .env.example .env</code></li>
<li>Set the platform path: <code>PLATFORM_PATH=/opt/1cv8/x86_64/8.3.27.2074</code></li>
<li>Start the containers: <code>docker compose up -d</code></li>
<li>Open port 8080 in the server firewall: <code>sudo ufw allow 8080/tcp</code></li>
</ol>
<p>Verify accessibility from the workstation: <code>curl http://server-erp:8080/health</code></p>

<h3>Connecting the AI assistant</h3>
<p>On the developer's workstation, use the server address instead of localhost:</p>
<pre><code># Codex
codex mcp add onec --url http://server-erp:8080/mcp

# Other MCP clients
# URL: http://server-erp:8080/mcp</code></pre>
<p>Here <code>server-erp</code> is the hostname or IP address of the 1C server (e.g. <code>192.168.1.100</code>).</p>
<p>The dashboard is also accessible remotely: <code>http://server-erp:8080/dashboard</code></p>

<h3>Connecting EPF</h3>
<p>Open the MCPToolkit.epf data processor in the 1C thin client on the server. In the "Gateway address" field enter <code>http://localhost:8080</code>, since the gateway and the data processor run on the same machine. Click "Connect".</p>
<p>If <code>GATEWAY_API_TOKEN</code> is enabled, use <code>http://localhost:8080#token=&lt;secret&gt;</code>. The token is sent as <code>Authorization: Bearer ...</code> for mutating API calls.</p>
<p>If the 1C client runs on the workstation (not on the server), enter the server address in the "Gateway address" field: <code>http://server-erp:8080</code> (or <code>http://server-erp:8080#token=&lt;secret&gt;</code> for protected API).</p>

<h3>What works in remote mode</h3>
<p>Most tools work without limitations. Everything that goes through EPF (data, metadata, event log) works the same way - locally and remotely. BSL navigation requires separate setup.</p>
<table>
<tr><th>Category</th><th>Tools</th><th>Status</th><th>Note</th></tr>
<tr><td>Data queries</td><td><code>execute_query</code>, <code>execute_code</code></td><td><span style="color:#22c55e">Works</span></td><td>All queries are executed through EPF on the server</td></tr>
<tr><td>Metadata</td><td><code>get_metadata</code>, <code>get_access_rights</code></td><td><span style="color:#22c55e">Works</span></td><td>Configuration structure and roles through EPF</td></tr>
<tr><td>Event log</td><td><code>get_event_log</code></td><td><span style="color:#22c55e">Works</span></td><td>Reading and filtering event log through EPF</td></tr>
<tr><td>Links and search</td><td><code>get_object_by_link</code>, <code>get_link_of_object</code>, <code>find_references_to_object</code></td><td><span style="color:#22c55e">Works</span></td><td>Navigation links and reference search</td></tr>
<tr><td>Documentation</td><td><code>search</code>, <code>info</code>, <code>getMembers</code>, <code>getMember</code>, <code>getConstructors</code></td><td><span style="color:#22c55e">Works</span></td><td>Documentation is stored in the container on the server</td></tr>
<tr><td>Query validation</td><td><code>validate_query</code></td><td><span style="color:#22c55e">Works</span></td><td>Static + server-side validation</td></tr>
<tr><td>Anonymization</td><td><code>enable_anonymization</code>, <code>disable_anonymization</code></td><td><span style="color:#22c55e">Works</span></td><td>PII masking on the gateway side</td></tr>
<tr><td>ITS search</td><td><code>its_search</code></td><td><span style="color:#22c55e">Works</span></td><td>1C:Naparnik API from the server container</td></tr>
<tr><td>Management</td><td><code>connect_database</code>, <code>switch_database</code>, <code>list_databases</code>, etc.</td><td><span style="color:#22c55e">Works</span></td><td>Database management through the gateway</td></tr>
<tr><td>BSL navigation</td><td><code>definition</code>, <code>symbol_explore</code>, <code>call_graph</code>, <code>hover</code>, etc.</td><td><span style="color:#eab308">Requires setup</span></td><td>BSL files must be exported to the server</td></tr>
<tr><td>BSL export</td><td><code>export_bsl_sources</code></td><td><span style="color:#22c55e">Works</span></td><td>1cv8 DESIGNER with automatic platform version detection</td></tr>
</table>

<h3>MCPToolkit.epf behavior</h3>
<p>When connecting, the data processor detects whether the gateway runs locally or remotely. If the gateway address differs from <code>localhost</code> or <code>127.0.0.1</code>, the data processor:</p>
<ul>
<li>Shows a warning in the event log: "Remote gateway, some features may be limited"</li>
<li>The "Export BSL" button works only if the export service (<code>export-host-service.py</code>) is running on the server</li>
<li>All other functions (connect, disconnect, command execution) work without limitations</li>
</ul>
<p>If the gateway is deployed on the same server where 1C runs, keep <code>http://localhost:8080</code> in the "Gateway address" field. In this case the data processor works in full mode.</p>
<p>When running via web client or remote thin client, the EPF automatically detects the remote connection and shows in the event log:</p>
<pre><code>Remote connection (web/thin client).
Available: queries, code, metadata, event log, access rights.
Unavailable: BSL navigation, BSL export.
For full functionality: docker compose on the 1C server.</code></pre>

<h3>Ports for remote access</h3>
<p>Minimum set of ports to open on the 1C server:</p>
<table>
<tr><th>Port</th><th>Service</th><th>Purpose</th></tr>
<tr><td><code>8080</code></td><td>MCP gateway</td><td>AI assistant connects to <code>/mcp</code>, dashboard at <code>/dashboard</code></td></tr>
<tr><td><code>443</code> or <code>80</code></td><td>1C web publication</td><td>Thin client in browser (usually already open)</td></tr>
<tr><td><code>3389</code></td><td>RDP (optional)</td><td>Remote desktop for Designer access</td></tr>
</table>
<p>For direct Designer access without RDP:</p>
<table>
<tr><th>Port</th><th>Purpose</th></tr>
<tr><td><code>1540-1541</code></td><td>1C cluster agent and manager</td></tr>
<tr><td><code>1560-1591</code></td><td>1C cluster worker processes</td></tr>
</table>
<p>All other ports (6003, 6100+, 8081) work inside the server via localhost and do not need to be opened externally.</p>

<h3>Setting up BSL navigation on a remote server</h3>
<p>BSL navigation (go-to-definition, symbol search, call graphs) only works when exported BSL files are present on the server. Without them, tools like <code>definition</code>, <code>symbol_explore</code>, <code>call_graph</code> will return empty results. There are two ways to get the BSL files:</p>

<p><b>Option 1. Export via EPF (automatic):</b></p>
<ol>
<li>Start the export service on the server: <code>python3 tools/export-host-service.py --port 8082</code></li>
<li>Make sure <code>.env</code> contains: <code>EXPORT_HOST_URL=http://localhost:8082</code></li>
<li>If the host service is unavailable and you intentionally use HASP/CI: set <code>ALLOW_CONTAINER_DESIGNER_EXPORT=true</code>. Container export is disabled by default.</li>
<li>Open MCPToolkit.epf in 1C, click "Export BSL"</li>
<li>Wait for completion (3-5 minutes for ERP, 1-2 minutes for smaller configurations)</li>
</ol>

<p><b>Option 2. Manual export and copy:</b></p>
<ol>
<li>Export the configuration to XML on any machine with the 1C platform: <code>ibcmd /S server-erp/ERP_Test /DumpConfigToFiles /home/user/export</code></li>
<li>Copy files to the server: <code>scp -r /home/user/export user@server-erp:/home/user/1c-projects/ERP_Test</code></li>
<li>Ask the AI assistant to reindex: "Reindex BSL files"</li>
</ol>
<p>After export, the AI gets full navigation access: go-to-definition, call graphs, symbol search, error diagnostics, renaming.</p>

<h3>Remote connection diagram</h3>
<pre><code>Workstation                          1C Server (server-erp)
+-----------------+                  +-----------------------------+
| Codex / other   |   HTTP :8080     | Docker                      |
| MCP client      | ---------------> | onec-mcp-gw (gateway)       |
|                 |   /mcp           | onec-toolkit (backend)      |
+-----------------+                  | platform-context            |
                                     | mcp-lsp (BSL navigation)    |
                                     +-----------------------------+
                                              ^
                                              | localhost
                                     +-----------------------------+
                                     | 1C Thin Client              |
                                     | MCPToolkit.epf              |
                                     +-----------------------------+
                                              |
                                     +-----------------------------+
                                     | 1C:Enterprise Server        |
                                     | Information Database        |
                                     +-----------------------------+</code></pre>

<div class="warn"><p><b>Security:</b> Port 8080 opens access to all gateway tools, including code execution and DB queries. Restrict access by IP address using the firewall. Do not open the port to the internet without additional protection (VPN, SSH tunnel). Example restriction: <code>sudo ufw allow from 192.168.1.0/24 to any port 8080</code></p></div>

<!-- ================================================================== -->
<h2 id="tools-en">6. MCP tools</h2>
<p>The gateway provides the AI assistant with the full toolset aggregated from all backends. The sections below contain the complete list of MCP tools by category. The exact number depends on whether <code>its_search</code> is enabled (<code>NAPARNIK_API_KEY</code>).</p>

<h3>1C Data (via onec-toolkit, 8 tools)</h3>
<table>
<tr><th>Tool</th><th>Description</th></tr>
<tr><td><code>execute_query</code></td><td>Execute a database query in 1C built-in query language. Supports parameters (<code>&amp;Parameter</code>), limits, nested queries. Automatically profiled and anonymized (if enabled).</td></tr>
<tr><td><code>execute_code</code></td><td>Execute arbitrary code in the 1C built-in language. Supports <code>execution_context=\"server\"</code> for CRUD/metadata access and <code>execution_context=\"client\"</code> for EPF form attributes and dangerous-toggle checks. Return values are passed through the <code>Результат</code> variable.</td></tr>
<tr><td><code>get_metadata</code></td><td>Get configuration metadata object structure: attributes, tabular sections, field types, string lengths, etc. Results are cached.</td></tr>
<tr><td><code>get_event_log</code></td><td>Read the 1C event log with filtering by date, event, user, severity level.</td></tr>
<tr><td><code>get_object_by_link</code></td><td>Get object data by 1C navigation link (format <code>e1cib/...</code>).</td></tr>
<tr><td><code>get_link_of_object</code></td><td>Generate a navigation link for an object. The live API expects <code>object_description</code> with an object-ref descriptor.</td></tr>
<tr><td><code>find_references_to_object</code></td><td>Find all places where an object is used. The live API expects <code>target_object_description</code> and <code>search_scope</code>.</td></tr>
<tr><td><code>get_access_rights</code></td><td>Analyze roles and access rights for metadata objects. Shows which roles have access and which operations are permitted.</td></tr>
</table>

<h3>1C reports by user-facing title (15 tools)</h3>
<table>
<tr><th>Tool</th><th>Description</th></tr>
<tr><td><code>analyze_reports</code></td><td>Builds a per-database SQLite report catalog from exported BSL/XML sources.</td></tr>
<tr><td><code>enrich_report_docs</code></td><td>Fetches and caches ITS/1C:Naparnik report descriptions to improve accountant-facing search.</td></tr>
<tr><td><code>find_reports</code></td><td>Finds a report by UI title, synonym, variant, or technical name.</td></tr>
<tr><td><code>list_reports</code></td><td>Lists cataloged reports for the explicit database. The <code>database</code> argument is required.</td></tr>
<tr><td><code>describe_report</code></td><td>Shows the technical report, variant, parameters, launch strategy, and output contract.</td></tr>
<tr><td><code>get_report_runner_policy</code></td><td>Returns the stored API/UI runner policy for a report or variant.</td></tr>
<tr><td><code>set_report_runner_policy</code></td><td>Stores the preferred runner: <code>api</code>, <code>ui</code>, or <code>auto</code>.</td></tr>
<tr><td><code>get_report_ui_strategy</code></td><td>Returns the web-client recipe for opening the report, filling fields, generating, and exporting.</td></tr>
<tr><td><code>set_report_ui_strategy</code></td><td>Stores the exact web-client recipe for a report or variant.</td></tr>
<tr><td><code>run_report</code></td><td>Runs a report by user-facing title. The <code>runner</code> argument selects <code>api</code>, <code>ui</code>, or <code>auto</code>.</td></tr>
<tr><td><code>validate_all_reports</code></td><td>Runs a quick catalog validation and records run statuses.</td></tr>
<tr><td><code>validate_report_contracts</code></td><td>Runs heavy per-variant validation, compares expected and observed result structure, and stores a campaign.</td></tr>
<tr><td><code>get_report_result</code></td><td>Fetches a stored large report result page by <code>run_id</code>.</td></tr>
<tr><td><code>get_report_validation_campaign</code></td><td>Fetches a stored heavy contract-validation campaign.</td></tr>
<tr><td><code>explain_report_strategy</code></td><td>Explains why a specific launch strategy was selected and shows the latest contract diagnostics.</td></tr>
</table>
<p><b>Architecture:</b> report tools combine a local SQLite catalog, BSL/XML/SKD analysis, output contracts, and two independent execution paths. The API runner uses <code>toolkit</code>/<code>EPF</code> without a browser. The UI runner uses the 1C web client: it opens the report, fills parameters, generates it, exports a temporary <code>XLSX</code> or <code>HTML</code> file, parses it, and deletes it after a successful read.</p>
<p><b>How to get data:</b> the usual flow is <code>find_reports</code> to search by user-facing title, <code>describe_report</code> to inspect parameters, variants, and the output contract, then <code>run_report</code> with explicit <code>database</code>, <code>runner=auto</code>, and, when needed, <code>period</code>, <code>filters</code>, <code>params</code>, and <code>context</code>. If the response is large, continue with <code>get_report_result</code>. If the runner returns <code>needs_input</code>, that is a structured request for missing parameters or runtime context rather than a hidden failure.</p>
<p><b>Output contracts:</b> <code>describe_report</code> returns <code>output_contract</code>. It describes the expected result shape: rows, artifact, columns, groupings, totals, and detail rows. After each run, the gateway builds an observed signature and compares it with the contract. If the API path cannot return a correct tabular result, and the UI runner plus fallback are enabled, <code>runner=auto</code> can move to the web UI.</p>
<p><b>Policies and recipes:</b> <code>set_report_runner_policy</code> chooses the preferred runner for a report or variant. <code>set_report_ui_strategy</code> stores the exact web-client recipe: how to open the report, which fields to fill, which button to click, and which export format to use.</p>
<p><b>Catalog refresh:</b> the catalog can be rebuilt explicitly through <code>analyze_reports</code>. It may also refresh after export and reindex operations when the gateway detects new exported sources.</p>
<div class="note"><p><b>Example:</b> <code>run_report(database="MY_INFOBASE", title="Расчетный листок", period={"from":"2025-12-01","to":"2025-12-31"}, filters={"Организация":"Основная"}, params={"ВариантОтчета":"Основной"}, runner="auto")</code>. Report tools always require explicit <code>database</code> so concurrently connected databases do not mix. For accountant phrasing like "payroll slip", call <code>enrich_report_docs</code> first; the static catalog still works without Naparnik.</p></div>

<h3>Platform Documentation (via platform-context, 5 tools)</h3>
<table>
<tr><th>Tool</th><th>Description</th></tr>
<tr><td><code>info</code></td><td>General information about a 1C platform type: description, purpose, base type.</td></tr>
<tr><td><code>getMembers</code></td><td>List of all properties and methods of a type (e.g. all methods of the <code>Query</code> object).</td></tr>
<tr><td><code>getMember</code></td><td>Detailed description of a specific method or property: parameters, return types, description.</td></tr>
<tr><td><code>getConstructors</code></td><td>Type constructor descriptions (creation variants via <code>New</code>).</td></tr>
<tr><td><code>search</code></td><td>Full-text search across platform documentation: types, methods, properties.</td></tr>
</table>
<p><b>Important:</b> the live platform-context schema is strict: <code>search</code> requires <code>query</code>, <code>type</code>, and <code>limit</code>; <code>info</code> requires <code>name</code> and <code>type</code>; <code>getMembers</code>/<code>getConstructors</code> require <code>typeName</code>; <code>getMember</code> requires <code>typeName</code> and <code>memberName</code>.</p>

<h3>BSL Code Navigation (via bsl-lsp-bridge, 14 tools)</h3>
<table>
<tr><th>Tool</th><th>Description</th></tr>
<tr><td><code>symbol_explore</code></td><td>Semantic search for symbols (procedures, functions, variables) in the BSL project code.</td></tr>
<tr><td><code>definition</code></td><td>Go to symbol definition. Shows the file and line where the procedure/function is declared.</td></tr>
<tr><td><code>hover</code></td><td>Information about the symbol at cursor: type, parameters, documentation.</td></tr>
<tr><td><code>call_hierarchy</code></td><td>Call tree: who calls this procedure (incoming) and what it calls (outgoing).</td></tr>
<tr><td><code>call_graph</code></td><td>Build a call graph with entry point detection. Shows call chains between modules.</td></tr>
<tr><td><code>document_diagnostics</code></td><td>Errors and warnings in a specific BSL file (syntax errors, style issues).</td></tr>
<tr><td><code>project_analysis</code></td><td>Overall project analysis: symbol count, inter-module relationships, structure.</td></tr>
<tr><td><code>lsp_status</code></td><td>BSL Language Server status: version, indexing state, number of indexed files.</td></tr>
<tr><td><code>prepare_rename</code></td><td>Check if a symbol can be renamed before execution.</td></tr>
<tr><td><code>rename</code></td><td>Rename a symbol (procedure, function, variable) in all usage locations.</td></tr>
<tr><td><code>selection_range</code></td><td>Determine selection range for a symbol (smart selection).</td></tr>
<tr><td><code>get_range_content</code></td><td>Get the content of a specified line range in a file.</td></tr>
<tr><td><code>code_actions</code></td><td>Available refactoring actions for a selected code fragment.</td></tr>
<tr><td><code>did_change_watched_files</code></td><td>Notify LSP about file changes (for re-indexing).</td></tr>
</table>

<h3>Gateway tools (19 tools + <code>its_search</code> when <code>NAPARNIK_API_KEY</code> is configured)</h3>
<table>
<tr><th>Tool</th><th>Description</th></tr>
<tr><td><code>get_server_status</code></td><td>Status of all MCP backends: availability, tool counts.</td></tr>
<tr><td><code>export_bsl_sources</code></td><td>Export 1C configuration sources via 1cv8 DESIGNER /DumpConfigToFiles. Auto-detects platform version.</td></tr>
<tr><td><code>connect_database</code></td><td>Connect a new 1C database: registration, Docker container creation.</td></tr>
<tr><td><code>disconnect_database</code></td><td>Soft-disconnect a database: stop runtime containers and backends, but keep the registry entry.</td></tr>
<tr><td><code>switch_database</code></td><td>Switch the active database for the current MCP session (per-session routing).</td></tr>
<tr><td><code>list_databases</code></td><td>List all registered databases and their connection statuses.</td></tr>
<tr><td><code>validate_query</code></td><td>Validate 1C query syntax without executing: static checks (parentheses, keywords) + server-side validation via <code>TOP 0</code>.</td></tr>
<tr><td><code>reindex_bsl</code></td><td>Force BSL Language Server re-indexing. Use after manual file changes, git pull, or external export.</td></tr>
<tr><td><code>write_bsl</code></td><td>Write a BSL module to the project with automatic LSP re-indexing.</td></tr>
<tr><td><code>bsl_index</code></td><td>Build a full-text search index over BSL sources: all procedures, functions, comments.</td></tr>
<tr><td><code>bsl_search_tool</code></td><td>Search for procedures and functions in the BSL index. Supports filtering by exported symbols.</td></tr>
<tr><td><code>enable_anonymization</code></td><td>Enable personal data masking in tool responses.</td></tr>
<tr><td><code>disable_anonymization</code></td><td>Disable personal data masking.</td></tr>
<tr><td><code>its_search</code></td><td>Search ITS documentation (1C Information and Technology Support) via the 1C:Naparnik API. Requires <code>NAPARNIK_API_KEY</code> configuration.</td></tr>
<tr><td><code>invalidate_metadata_cache</code></td><td>Clear the metadata cache. Equivalent to the "Clear Cache" button in the dashboard.</td></tr>
<tr><td><code>query_stats</code></td><td>Query performance statistics: count, avg/max time, error rate.</td></tr>
<tr><td><code>graph_stats</code></td><td>Built-in BSL dependency graph statistics: node count, edge count, distribution by object types. Requires the <code>bsl-graph</code> profile.</td></tr>
<tr><td><code>graph_search</code></td><td>Search for configuration objects in the built-in dependency graph. Requires the <code>bsl-graph</code> profile.</td></tr>
<tr><td><code>graph_related</code></td><td>Find related objects in the built-in dependency graph: what the object uses and what uses it. Requires the <code>bsl-graph</code> profile.</td></tr>
</table>

<h3>Graph Web UI</h3>
<p>When the <code>bsl-graph</code> profile is running, a built-in interactive graph viewer is served at <a href="http://localhost:8888/" target="_blank" rel="noopener"><code>http://localhost:8888/</code></a> (cytoscape.js, bundled, works offline). It uses the same slate palette and typography as this dashboard; clicking the link from the <i>Optional Services</i> card auto-applies the current RU/EN locale.</p>
<p><b>Toolbar:</b> a search field with whitespace-token matching (the query "размеры пособий" / "salary deduction" matches names that contain every token); ◀ ▶ walk the search history like a browser; <b>List</b> opens a floating panel with the last search's hits (clicking a row animates the camera to the node); <b>Clear</b> empties the canvas; <b>Rebuild</b> runs a synchronous <code>POST /api/graph/rebuild</code>. The <b>RU/EN</b> selector switches the UI language (full page reload).</p>
<p>The <b>left panel</b> has <i>Databases</i> (radio buttons — exactly one DB at a time, first one picked by default; when there is a single registered DB the radio is disabled and purely informational) and <i>Types</i> (counts for the selected DB, click a type to load its first 30 nodes). The <b>centre canvas</b> runs cytoscape.js with a <code>cose</code> layout, 600–700ms ease-out animation, smooth wheel zoom (<code>wheelSensitivity: 0.2</code>). Node fill encodes the object type, border encodes the database. Long CamelCase names auto-break on case boundaries. Single click selects a node — details on the right plus neighbourhood highlighting (the rest of the graph dims). Double-click drills in via <code>/api/graph/related/&lt;id&gt;</code>. The canvas caps at 200 nodes.</p>
<p>The <b>right panel</b> shows node details (type, db, id, path) plus an <b>Expand neighbours</b> button, and a legend mapping border colour → database. The <b>floating List panel</b> opens from the toolbar and shows every hit from the last search with coloured dots (fill = type, border = db).</p>
<p><b>Gateway integration:</b> the gateway triggers <code>POST /api/graph/rebuild</code> automatically after every successful BSL export, so the graph stays fresh without waiting for the background rescan. <code>.bsl</code> files get readable display names like <code>ObjectName/Form/Module.bsl</code> (top-level type folder and <code>Ext</code> segments stripped) so search results stay distinguishable. In the <i>Optional Services</i> card the <code>bsl-graph</code> name becomes a clickable link when the container is running and plain text when stopped. When <code>BSL_WORKSPACE</code> / <code>BSL_HOST_WORKSPACE</code> is changed from the dashboard, the gateway recreates <code>bsl-graph</code> automatically so Docker picks up the new bind mount. If you edit <code>.env</code> manually or bypass the dashboard, use <code>docker compose --profile bsl-graph up -d --build --force-recreate bsl-graph</code> as the fallback.</p>

<h3>MCP Resource</h3>
<p>The gateway also provides one MCP resource: <code>syntax_1c.txt</code>, a BSL (1C built-in language) syntax reference: types, operators, control structures, procedures, exceptions, preprocessor directives. AI uses it as context when writing BSL code.</p>

<!-- ================================================================== -->
<h2 id="api-en">7. API Endpoints</h2>
<p>Complete list of gateway HTTP endpoints. The main entry point for AI assistants is <code>/mcp</code>. Other endpoints are used by the dashboard, the EPF data processor, or external systems.</p>
<table>
<tr><th>Path</th><th>Method</th><th>Description</th></tr>
<tr><td><code>/mcp</code></td><td>POST/GET</td><td>MCP Streamable HTTP, main entry point for AI assistants. Supports stateful sessions with an 8-hour idle timeout.</td></tr>
<tr><td><code>/health</code></td><td>GET</td><td>JSON status of all backends. Returns <code>{"status":"ok"}</code> if all backends are available, <code>{"status":"degraded"}</code> if at least one is unavailable.</td></tr>
<tr><td><code>/dashboard</code></td><td>GET</td><td>Web UI dashboard. Parameter <code>?lang=ru|en</code> for language switching.</td></tr>
<tr><td><code>/dashboard/docs</code></td><td>GET</td><td>This documentation page. Parameter <code>?lang=ru|en</code>.</td></tr>
<tr><td><code>/dashboard/diagnostics</code></td><td>GET</td><td>Full diagnostic report in JSON format (opens in a new tab).</td></tr>
<tr><td><code>/api/export-bsl</code></td><td>POST</td><td>REST endpoint for BSL source export. Called by MCPToolkit.epf when "Export BSL" is clicked. Body: <code>{"connection":"...","output_dir":"..."}</code>.</td></tr>
<tr><td><code>/api/reports/{action}</code></td><td>POST</td><td>REST facade over the report tools: <code>analyze</code>, <code>find</code>, <code>list</code>, <code>describe</code>, <code>runner-policy</code>, <code>set-runner-policy</code>, <code>ui-strategy</code>, <code>set-ui-strategy</code>, <code>run</code>, <code>validate-all</code>, <code>validate-contracts</code>, <code>result</code>, <code>campaign</code>, <code>explain</code>. The contract mirrors the corresponding MCP report tools.</td></tr>
<tr><td><code>/api/register</code></td><td>POST</td><td>EPF registration in the gateway. Called by MCPToolkit.epf when "Connect" is clicked. Body: <code>{"name":"...","connection":"...","channel":"..."}</code>; <code>channel</code> is optional but recommended for exact routing to the currently opened form instance. If the database is not connected, auto-connects it.</td></tr>
<tr><td><code>/api/unregister</code></td><td>POST</td><td>EPF unregistration. Called when "Disconnect" is clicked in the data processor. Body: <code>{"name":"..."}</code>.</td></tr>
<tr><td><code>/api/action/connect-db</code></td><td>POST</td><td>Connect a database from the dashboard. Body: <code>{"name":"...","connection":"...","project_path":"..."}</code>.</td></tr>
<tr><td><code>/api/action/disconnect</code></td><td>POST</td><td>Disconnect a database. Parameter: <code>?name=db_name</code>.</td></tr>
<tr><td><code>/api/action/switch</code></td><td>POST</td><td>Set the default database. Parameter: <code>?name=db_name</code>.</td></tr>
<tr><td><code>/api/action/edit-db</code></td><td>POST</td><td>Edit database parameters. Body: <code>{"name":"...","connection":"...","project_path":"..."}</code>.</td></tr>
<tr><td><code>/api/action/clear-cache</code></td><td>POST</td><td>Clear metadata cache.</td></tr>
<tr><td><code>/api/action/toggle-anon</code></td><td>POST</td><td>Toggle anonymization (on/off).</td></tr>
<tr><td><code>/api/action/get-env</code></td><td>POST</td><td>Read the <code>.env</code> file contents for display in the dashboard editor.</td></tr>
<tr><td><code>/api/action/save-env</code></td><td>POST</td><td>Save the <code>.env</code> file with automatic gateway restart. The dashboard submits the full file in replace mode: <code>{"content":"...","mode":"replace"}</code>. Sparse patch updates are rejected.</td></tr>
</table>

<!-- ================================================================== -->
<h2 id="env-en">8. Environment Variables (.env)</h2>
<p>All parameters are configured via the <code>.env</code> file in the project root. The file is mounted into the gateway container. You can edit it from the dashboard (Parameters tab, "Edit") or directly on the host.</p>
<table>
<tr><th>Variable</th><th>Default</th><th>Description</th></tr>
<tr><td><code>PORT</code></td><td>8080</td><td>Port the MCP gateway listens on. The AI assistant connects to <code>http://localhost:{PORT}/mcp</code>.</td></tr>
<tr><td><code>LOG_LEVEL</code></td><td>INFO</td><td>Logging level: <code>DEBUG</code> (maximum detail), <code>INFO</code>, <code>WARNING</code>, <code>ERROR</code> (errors only). Set to <code>DEBUG</code> for troubleshooting.</td></tr>
<tr><td><code>LOG_JSON</code></td><td>false</td><td>Enables structured JSON logging. Useful for ELK/Loki/Cloud logging pipelines; plain human-readable format stays default.</td></tr>
<tr><td><code>ONEC_TOOLKIT_URL</code></td><td>http://onec-toolkit:6003/mcp</td><td>Static onec-toolkit backend URL inside the Docker network. Change only if using a non-standard container configuration.</td></tr>
<tr><td><code>PLATFORM_CONTEXT_URL</code></td><td>http://platform-context:8080/sse</td><td>Platform documentation backend URL inside the Docker network.</td></tr>
<tr><td><code>ENABLED_BACKENDS</code></td><td>onec-toolkit,platform-context,bsl-lsp-bridge</td><td>Comma-separated list of enabled backends. To add YaXUnit, specify: <code>onec-toolkit,platform-context,bsl-lsp-bridge,test-runner</code>.</td></tr>
<tr><td><code>EXPORT_HOST_URL</code></td><td>http://localhost:8082</td><td>URL of the BSL export service running on the host (<code>tools/export-host-service.py</code>). On Windows: <code>http://host.docker.internal:8082</code>.</td></tr>
<tr><td><code>DOCKER_CONTROL_URL</code></td><td>http://localhost:8091</td><td>URL of the internal <code>docker-control</code> sidecar used by the gateway for container lifecycle, Docker diagnostics, and LSP stdio proxying. On Windows/macOS it becomes <code>http://docker-control:8091</code> inside the bridge network; host port <code>8091</code> is not published.</td></tr>
<tr><td><code>DOCKER_CONTROL_TOKEN</code></td><td>auto</td><td>Shared bearer token for internal <code>gateway -> docker-control</code> requests. <code>GET /health</code> remains public, while all <code>/api/*</code> sidecar calls require <code>Authorization: Bearer ...</code>. The dashboard editor masks the value.</td></tr>
<tr><td><code>ANONYMIZER_SALT</code></td><td>auto</td><td>Secret salt for stable PII masking across gateway restarts. Generated by <code>setup.sh</code> and masked in the dashboard editor.</td></tr>
<tr><td><code>ALLOW_CONTAINER_DESIGNER_EXPORT</code></td><td>false</td><td>Safety override. By default, running <code>1cv8</code> export inside Docker is disabled to protect software licenses. Set <code>true</code> only for HASP/CI scenarios.</td></tr>
<tr><td><code>PLATFORM_PATH</code></td><td>/opt/1cv8/x86_64/8.3.27.2074</td><td>Path to the 1C platform. Used for BSL export. The gateway auto-scans /opt/1cv8/x86_64/ to find the right 1cv8 version.</td></tr>
<tr><td><code>BSL_WORKSPACE</code></td><td>/workspace</td><td>BSL source working directory inside the LSP container. Configuration sources are mounted here.</td></tr>
<tr><td><code>BSL_HOST_WORKSPACE</code></td><td>—</td><td>Path to BSL sources on the host. Used for path conversion between container and host (so AI sees real file paths).</td></tr>
<tr><td><code>BSL_LSP_COMMAND</code></td><td>—</td><td>Command for direct BSL Language Server launch in all-in-one mode. In the regular gateway architecture, LSP containers are created automatically for each connected database.</td></tr>
<tr><td><code>NAPARNIK_API_KEY</code></td><td>—</td><td>API key for 1C:Naparnik (ITS search). Get it at <a href="https://code.1c.ai">code.1c.ai</a> -> Profile -> API token. Requires an ITS subscription.</td></tr>
<tr><td><code>TOOLKIT_ALLOW_DANGEROUS_WITH_APPROVAL</code></td><td>true</td><td>Allows the toolkit proxy to forward dangerous write/exec commands into 1C. The final allow/deny decision is still made by <code>MCPToolkit.epf</code> using its approval checkboxes and confirmation dialog. Set <code>false</code> only if you want the proxy to hard-block such commands before they reach 1C.</td></tr>
<tr><td><code>GATEWAY_RATE_LIMIT_ENABLED</code></td><td>true</td><td>Enables rate limiting for REST API and dashboard routes. <code>/mcp</code> and <code>/health</code> are intentionally excluded.</td></tr>
<tr><td><code>GATEWAY_RATE_LIMIT_READ_RPM</code></td><td>120</td><td>Per-minute read limit for <code>/api/*</code> and <code>/dashboard*</code> routes.</td></tr>
<tr><td><code>GATEWAY_RATE_LIMIT_MUTATING_RPM</code></td><td>30</td><td>Per-minute mutating limit for dashboard/API write operations.</td></tr>
<tr><td><code>METADATA_CACHE_TTL</code></td><td>600</td><td>Metadata cache lifetime in seconds. 600 = 10 minutes. Value of <code>0</code> completely disables caching.</td></tr>
<tr><td><code>TEST_RUNNER_URL</code></td><td>http://localhost:8000/sse</td><td>URL of the mcp-onec-test-runner backend for YaXUnit test execution. Optional.</td></tr>
<tr><td><code>BSL_GRAPH_URL</code></td><td>http://localhost:8888</td><td>URL of the built-in dependency graph service. Optional; enabled by the <code>bsl-graph</code> profile. When the service is running, a built-in web visualization (cytoscape.js, works offline) is served at this URL; the "Optional Services" card turns <code>bsl-graph</code> into a clickable link.</td></tr>
<tr><td><code>PLATFORM_PATH</code></td><td>/opt/1cv8/x86_64/8.3.27.2074</td><td>Full path to the specific 1C platform version directory. Used by the platform-context backend to read documentation.</td></tr>
<tr><td><code>HOST_PLATFORM_PATH</code></td><td>/opt/1cv8</td><td>Root path to the 1C platform on the host. Mounted into containers for access to ibcmd and other utilities.</td></tr>
<tr><td><code>ONEC_TIMEOUT</code></td><td>180</td><td>Timeout for 1C command execution (seconds). If a query or code does not complete within this time, a timeout error is returned.</td></tr>
</table>

<!-- ================================================================== -->
<h2 id="diagnostics-en">9. Diagnostics</h2>
<p>The "Diagnostics" link is located at the bottom of the dashboard (footer). It opens a full diagnostic report in a new browser tab in JSON format.</p>

<h3>Report Contents</h3>
<table>
<tr><th>Section</th><th>Contents</th></tr>
<tr><td><b>gateway</b></td><td>Gateway version, port, number of active MCP sessions, idle timeout setting.</td></tr>
<tr><td><b>backends</b></td><td>Status of each backend: availability, tool count, errors. Equivalent to the <code>/health</code> endpoint.</td></tr>
<tr><td><b>databases</b></td><td>List of all connected databases: name, connection string, project path, EPF status, toolkit backend port, LSP container name.</td></tr>
<tr><td><b>profiling</b></td><td>execute_query statistics: count, avg/max/min time, slow queries, error rate.</td></tr>
<tr><td><b>cache</b></td><td>Metadata cache state: entry count, hit rate, TTL, memory size.</td></tr>
<tr><td><b>anonymization</b></td><td>Whether anonymization is enabled, number of replacements in the current session.</td></tr>
<tr><td><b>docker</b></td><td>Docker daemon information: version, OS, CPU count, RAM, image and volume sizes.</td></tr>
<tr><td><b>containers</b></td><td>List of project containers: name, image, status, start time.</td></tr>
<tr><td><b>config</b></td><td>All environment variables (secrets are masked as <code>***</code>).</td></tr>
<tr><td><b>container_logs</b></td><td>Last 10 lines of logs from each project container. Helps quickly see errors without using <code>docker logs</code>.</td></tr>
</table>

<h3>When to Use</h3>
<ul>
<li>When investigating the cause of a problem - the report contains all necessary information in one place</li>
<li>When seeking help - copy the JSON report and attach it to the problem description</li>
<li>For monitoring - periodic check of all component states</li>
</ul>
<p>Diagnostics are also available via the API: <code>POST /api/action/diagnostics</code>.</p>

<!-- ================================================================== -->
<h2 id="troubleshooting-en">10. Troubleshooting</h2>

<h3>Backend shows red status</h3>
<p><b>Symptom:</b> On the Status tab, one or more backends display a red indicator.</p>
<p><b>Causes and solutions:</b></p>
<ol>
<li>Container is not running. Check: <code>docker compose ps</code>. If the container is stopped, start it: <code>docker compose up -d</code>.</li>
<li>Container crashed with an error. Check logs: <code>docker logs onec-mcp-gw -f</code> (for the gateway), <code>docker logs onec-mcp-toolkit -f</code> (for toolkit). A common cause is out of memory.</li>
<li>Network issue between containers. Verify that containers are in the same Docker network: <code>docker network ls</code>.</li>
</ol>

<h3>EPF data processor not connecting</h3>
<p><b>Symptom:</b> In the MCPToolkit.epf data processor, clicking "Connect" results in an error or the status remains "Not connected".</p>
<p><b>Causes and solutions:</b></p>
<ol>
<li>Gateway is not running. Check: <code>curl http://localhost:8080/health</code>. Should return JSON.</li>
<li>Wrong gateway address in the data processor. Verify that the "Gateway address" field contains the correct URL (default <code>http://localhost:8080</code>).</li>
<li>Firewall is blocking the port. On Windows, check whether port 8080 is allowed in firewall settings.</li>
<li>1C is running in thin client mode with HTTP restrictions. Try using the thick client.</li>
</ol>

<h3>BSL navigation not working</h3>
<p><b>Symptom:</b> The <code>definition</code>, <code>symbol_explore</code>, <code>call_hierarchy</code> tools return empty results or errors.</p>
<p><b>Causes and solutions:</b></p>
<ol>
<li>Sources not exported. Click "Export BSL" in the MCPToolkit.epf data processor. Wait for completion (3-5 minutes for large configurations).</li>
<li>LSP is still indexing files. Check status: call the <code>lsp_status</code> tool. Indexing large projects (ERP - 18,000 modules) takes several minutes.</li>
<li>LSP container is not running. Check: <code>docker ps | grep mcp-lsp</code>.</li>
<li>Project path is incorrect. Verify that the BSL source directory on the host contains <code>.bsl</code> files and is correctly mounted into the container.</li>
</ol>

<h3>.env edit error in dashboard</h3>
<p><b>Symptom:</b> When clicking "Edit" in the gateway configuration, the message ".env file not found" appears.</p>
<p><b>Causes and solutions:</b></p>
<ol>
<li>.env file does not exist. Create it: <code>cp .env.example .env</code> in the project root.</li>
<li>File is not mounted into the container. Verify that the gateway receives <code>./.env:/data/.env:ro</code> and <code>docker-control</code> receives <code>./.env:/data/.env:rw</code> in <code>docker-compose.yml</code>.</li>
<li>No write permissions. Check permissions: <code>ls -la .env</code>. The file must be writable.</li>
</ol>

<h3>AI receives outdated metadata</h3>
<p><b>Symptom:</b> AI claims an object has an attribute that doesn't exist (or vice versa).</p>
<p><b>Causes and solutions:</b></p>
<ol>
<li>Metadata cache contains stale data. Clear the cache: "Clear Cache" button on the Settings tab or the <code>invalidate_metadata_cache</code> tool.</li>
<li>Reduce cache TTL in settings: <code>METADATA_CACHE_TTL=60</code> (1 minute instead of 10).</li>
</ol>

<h3>execute_query queries run slowly</h3>
<p><b>Symptom:</b> Queries take more than 5 seconds, the Profiling card shows large numbers.</p>
<p><b>Causes and solutions:</b></p>
<ol>
<li>Suboptimal queries from AI. Check the <code>_profiling</code> field in the response - it contains hints (SELECT *, missing WHERE, many JOINs).</li>
<li>Load on the 1C server. Queries execute in the database context and compete with other users.</li>
<li>Network latency. If the 1C server is on a remote host, check ping and bandwidth.</li>
<li>Increase timeout: <code>ONEC_TIMEOUT=300</code> (5 minutes instead of 3).</li>
</ol>

<h3>Data processor shows "No response from gateway" (timeout)</h3>
<p><b>Symptom:</b> MCPToolkit.epf periodically outputs a timeout error to the event log.</p>
<p><b>Causes and solutions:</b></p>
<ol>
<li>Gateway is overloaded or restarting. Check logs: <code>docker logs onec-mcp-gw --tail 50</code>.</li>
<li>Long-polling connection was broken. Click "Disconnect" and then "Connect" again.</li>
<li>On the very first connection after a cold start, allow more time than usual: EPF registration now permits up to 240 seconds while the gateway prepares runtime for the database.</li>
<li>Docker network issues. Restart containers: <code>docker compose restart</code>.</li>
</ol>

<h3>Docker containers not created when connecting a database</h3>
<p><b>Symptom:</b> When connecting a database, the error "Failed to create container" appears.</p>
<p><b>Causes and solutions:</b></p>
<ol>
<li>Insufficient Docker resources. Check free space: <code>docker system df</code>. Clean up unused images: <code>docker system prune</code>.</li>
<li><code>docker-control</code> is unavailable. On Linux, check <code>http://127.0.0.1:8091/health</code>. On Windows/macOS, use <code>./verify-install.sh</code> / <code>verify-install.ps1</code> because the sidecar is checked from inside <code>onec-mcp-gw</code>. Ensure <code>DOCKER_CONTROL_TOKEN</code> exists in <code>.env</code>. Direct <code>docker.sock</code> access is no longer used by the gateway.</li>
<li>Name conflict. A container with that name already exists. Remove manually: <code>docker rm -f onec-toolkit-{db_name}</code>.</li>
</ol>

</body></html>""",
}


def render_docs(lang: str = "ru") -> str:
    return DOCS_HTML.get(lang, DOCS_HTML["ru"])
